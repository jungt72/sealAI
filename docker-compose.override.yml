version: "3.9"

services:
  postgres:
    env_file:
      - .env.dev

  keycloak:
    env_file:
      - .env.dev

  backend:
    env_file:
      - backend/.env
      - .env.dev
    environment:
      REDIS_URL: "redis://redis:6379/0"
      RAG_BM25_ENABLED: "0"
      RAG_BM25_INDEX: "sealai:bm25"
      ENABLE_LANGGRAPH_V06: "true"

  frontend:
    # env_file bewusst geleert, damit .env.dev NICHT greift
    env_file: []
    environment: {}

  nginx:
    env_file:
      - .env.dev
