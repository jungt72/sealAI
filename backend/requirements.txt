###############################################################################
# SEALAI - BACKEND REQUIREMENTS (Python 3.11+)
#
# Architektur:
# - Async FastAPI Runtime (asyncpg)
# - LangGraph v2 (Redis Checkpoint, Supervisor)
# - RAG via Qdrant (Single-Collection, tenant-scoped)
# - Alembic mit *sync* DB-Driver (psycopg2) für Migrationen
#
# Prinzipien:
# - Reproduzierbare Builds (strict pinning)
# - Keine impliziten / ungenutzten Abhängigkeiten
# - Async im Runtime, Sync nur für Alembic
###############################################################################

# =============================================================================
# Web Framework / Runtime
# =============================================================================
fastapi==0.128.0
starlette==0.50.0
uvicorn[standard]==0.40.0
websockets==16.0

httpx==0.28.1
httpcore==1.0.9
anyio==4.12.1

orjson==3.11.5
python-dotenv==1.2.1
PyYAML==6.0.3

pydantic==2.12.5
pydantic-core==2.41.5
pydantic-settings==2.12.0
typing-extensions==4.15.0
tenacity==9.1.2

# =============================================================================
# OpenAI / Tokenization
# =============================================================================
openai==2.16.0
tiktoken==0.12.0

# =============================================================================
# LangChain / LangGraph (v2)
# =============================================================================
langchain==1.2.7
langchain-core==1.2.7
langchain-community==0.4.1
langchain-openai==1.1.7
langchain-text-splitters==1.1.0
langchain-qdrant==1.1.0
langsmith==0.6.6

# LangGraph v2 Core
langgraph==1.0.7
langgraph-checkpoint==3.0.1
langgraph-checkpoint-redis==0.3.3
langgraph-supervisor==0.0.31

# Optional – nur aktivieren, wenn explizit importiert
# langgraph-prebuilt==1.0.5
# langgraph-sdk==0.3.2

# =============================================================================
# Vector DB / Retrieval
# =============================================================================
qdrant-client==1.16.2
redis==7.1.0

# Lokale Embeddings (RAG Ingest & Query)
fastembed==0.7.4
onnxruntime==1.23.2

# =============================================================================
# Datenbanken
# =============================================================================
SQLAlchemy==2.0.46
alembic==1.18.1

# Async Runtime DB Driver (App)
asyncpg==0.31.0     # postgresql+asyncpg://

# Sync DB Driver (Alembic / Migrations ONLY)
psycopg2-binary==2.9.11   # postgresql+psycopg2://

# =============================================================================
# Auth / Security
# =============================================================================
python-keycloak==7.0.3
python-jose[cryptography]==3.5.0
cryptography==46.0.4

# =============================================================================
# Logging / Observability
# =============================================================================
structlog==25.5.0

# =============================================================================
# Prompting / Templates
# =============================================================================
Jinja2==3.1.6

# =============================================================================
# File Parsing / RAG Ingestion
# =============================================================================
python-multipart==0.0.22   # FastAPI Uploads
docx2txt==0.9
python-docx==1.2.0
pypdf==5.6.0

###############################################################################
# Hinweise:
# - Alembic nutzt POSTGRES_SYNC_URL (psycopg2)
# - Runtime nutzt DATABASE_URL (asyncpg)
###############################################################################
