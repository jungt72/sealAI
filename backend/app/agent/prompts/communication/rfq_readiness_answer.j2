{% if mode == "no_active_case" -%}
Ich kann eine Anfragebasis oder RFQ-Basis vorbereiten, aber dafür brauche ich zuerst einen qualifizierten Dichtungsfall. Ohne Fall, Revision und technische Eckdaten erstelle ich keine Herstelleranfrage und sende nichts extern.

Der sichere nächste Schritt ist die Qualifikation: Um welche Dichtung oder Anwendung geht es?
{%- elif mode == "readiness_ready" -%}
Deine Anfragebasis wirkt aus dem aktuellen Arbeitsstand grundsätzlich als Anfrageentwurf für eine Herstellerprüfung vorbereitet. Das ist keine technische Endentscheidung und kein automatischer Herstellerentscheid; vor externem Teilen braucht es weiterhin den geregelten RFQ-/Consent-Schritt.

Ich sende nichts automatisch an Hersteller.
{%- elif mode == "readiness_open" -%}
Die Anfragebasis ist für eine Herstellerprüfung noch nicht vollständig genug. Ich kann den RFQ-Status nur aus dem vorhandenen Arbeitsstand ableiten und erfinde keine fehlenden Werte.

Offene Punkte: {{ open_points }}.

Das ist eine Anfragebasis für die Klärung mit einem Hersteller oder Spezialisten, keine technische Endentscheidung.
{% if pending_question %}

Der nächste sinnvolle Schritt bleibt: {{ pending_question }}
{% endif %}
{%- elif mode == "build_ready" -%}
Ich kann die {{ artifact }} als prüffähigen Anfrageentwurf vorbereiten, aber die dauerhafte Anfragevorschau entsteht erst über die explizite Aktion "Anfragevorschau vorbereiten". Das Teilen oder Exportieren läuft danach über den geregelten Preview- und Consent-Fluss. Ich sende nichts automatisch extern und treffe keine technische Endentscheidung.
{%- elif mode == "build_open" -%}
Ich kann daraus eine {{ artifact }} vorbereiten, aber im aktuellen Fall fehlen noch Angaben für eine belastbare Herstellerprüfung. Offene Punkte: {{ open_points }}.

Sobald die Pflichtangaben im geregelten Fall sauber vorliegen, kann daraus eine RFQ-Basis für die Herstellerprüfung entstehen. Die dauerhafte Anfragevorschau wird über die explizite Aktion "Anfragevorschau vorbereiten" erstellt. Das ist keine technische Endentscheidung und kein automatischer Versand.
{% if pending_question %}

Der nächste sinnvolle Schritt bleibt: {{ pending_question }}
{% endif %}
{%- elif mode == "contact" -%}
{{ status }}. {{ dispatch_note }}

Ich kann die Anfragebasis für eine Herstellerprüfung vorbereiten und offene Punkte sichtbar machen, aber ich kontaktiere keinen Hersteller automatisch und treffe keine technische Endentscheidung.
{% if pending_question and has_missing %}

Der nächste sinnvolle Schritt bleibt: {{ pending_question }}
{% endif %}
{%- endif %}
