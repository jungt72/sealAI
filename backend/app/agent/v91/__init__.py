"""SealAI V9.1 runtime contracts and adapters.

The V9.1 layer is introduced as an explicit policy vocabulary around the
existing V7/V8 runtime. It must not bypass RuntimeAction or mutate case truth.
"""
