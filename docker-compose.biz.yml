# BLOCKED_EXTERNAL: the former Strapi/Odoo stack had floating images, shared
# production networks and static database credentials.  It is intentionally
# inert until an independently approved, attested release topology exists.
name: sealai-biz-blocked-external
services: {}
