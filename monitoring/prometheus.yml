global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs: []

# The current production backend image does not register a Prometheus
# `/metrics` route, so scraping it only produces persistent 404 noise.
# Re-enable a backend scrape job when a backend release explicitly exposes
# a supported metrics endpoint.
