# Production hostname, proxy headers and the DB credential are runtime settings
# in docker-compose.deploy.yml. This file intentionally contains no deployment-
# specific hostname or secret.
db=postgres
health-enabled=true
metrics-enabled=true
