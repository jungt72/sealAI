# Generated by gen-staging-conf.sh — never committed (it is a derived copy of nginx/default.conf).
conf/

# Host-local RC inputs. The example is intentionally non-runnable and may be committed;
# the real contract and generated TLS fixtures must remain local to the isolated RC host.
rc.env
!rc.env.example
tls/
www/
