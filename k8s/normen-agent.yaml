apiVersion: apps/v1
kind: Deployment
metadata:
  name: normen-agent
  namespace: sealai
spec:
  replicas: 1
  selector:
    matchLabels: {app: normen-agent}
  template:
    metadata:
      labels: {app: normen-agent}
    spec:
      containers:
        - name: normen-agent
          image: ghcr.io/jungt72/sealai-normen-agent:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: UVICORN_HOST
              value: 0.0.0.0
            - name: UVICORN_PORT
              value: "9101"
          command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "9101"]
          ports:
            - containerPort: 9101
          readinessProbe:
            httpGet: {path: /health, port: 9101}
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: normen-agent
  namespace: sealai
spec:
  selector: {app: normen-agent}
  ports:
    - name: http
      port: 9101
      targetPort: 9101

