# V1.7 Full Deep-Dive Audit #3 + Repo-Professionalität

**Date:** 2026-06-05 · **Branch HEAD:** demo `180fd271` · **Live digests:** backend
`ghcr.io/jungt72/sealai-backend:ccdd4577-20260605-060228`, frontend
`d40d7145-20260604-183558` · **Method:** FAILS-until-proven, both-direction
falsification, evidence grammar `[E]` (direct, `path:line`) / `[A]` (inference) /
`[NV]` (claimed-but-unverified); every verdict re-derived on live code. Prior audits
(`2026-06-03_v17_gap_audit.md`, `2026-06-04_v17_gap_audit_rerun.md`) and all closeout
claims (ERFÜLLT-markings, "resolved", GOVERNANCE_LOG entries) are themselves audit
subject. Read-only audit — no fixes were applied.

## 0. Scope & provenance caveat (read first)

- **AC 1–10 are defined OFF-BRANCH only [E]/[A]:** `§11 "Acceptance Criteria V1.7"`
  exists solely on `origin/feat/v1.7-blueprint` (commit `c9c3b47b`), **not** on the
  runtime branch. `AGENTS.md` binds **V1.6 / RWDR-MVP**, not "V1.7-Universal" — so
  the entire V1.7 bar audited here is `[A]` aspirational, not the binding contract.
- **S1–S5 are auditor-coined [A]:** not in any blueprint; introduced by the 06-03
  audit. S4 cites a "§20" that does not exist in the off-branch V1.7 file (`[NV]`).
- The on-branch "§11" (`..._v1_6_..._blueprint.md:1747`) is **"Multi-Output Contract"**,
  a different section — do not conflate. `BLUEPRINT_V1_7_FINAL_CLOSEOUT.md` is a
  *different* V1.7 ("Advanced Domain Logic & RAG Coupling") and contains no AC/S — decoy.

### What was independently re-verified for this audit (read-only)

- **deployed==merged [E]:** live backend digest tag references `ccdd4577`; demo HEAD
  is `180fd271`; the 2-commit lag (`d16487ef`, `180fd271`) is **docs-only**
  (`GOVERNANCE_LOG.md` + `runtime-audit-fixmap.md`); `git diff ccdd4577..180fd271 --
  backend/ frontend/` is **empty** → deployed backend/frontend code == merged demo
  code. Frontend digest `d40d7145`; `git diff d40d7145..demo -- frontend/` empty.
- **main ⊇ demo [E]:** `gh api .../compare/main...demo` → `behind_by:7,
  total_commits:0` → origin/main contains all of demo (local refs were stale).
- **v1.7.0 tag is fine [E]:** `009bd55f` (annotated-tag object) wraps commit
  `bffa2188` (PR #11 convergence); local==remote. Not a finding.
- **C1 holds [E]:** zero live `== "rwdr"` equality/membership branches in the named
  core files; the only hit is a *motion*-label branch (`case_workspace.py:2229
  motion_label in {"rotary","linear","static"}`), correctly outside the seal-type token set.
- **C10 echo is live [E]:** caller `rwdr_mvp_brief.py:2231` → brief `:2247` →
  projection `:4060-4075`, L1-guarded with safe fallback `:2884`.
- **Four doctrine repros still block at L1 [E]:** regex-executed — "EPDM könnte
  optimal sein"/"PTFE ist NBR überlegen"/"PTFE übertrifft NBR"/"EPDM ist optimal für
  diese Anwendung" all match `output_guard.py:109/117/127/113`.
- **NEW [E]:** no CI workflow runs pytest / the architecture enforcers / doctrine suite (Risk #2).

## 1. SCOPE A — V1.7 acceptance matrix (diff vs BOTH prior audits)

Verdict legend: ✅ erfüllt · ✅* erfüllt-per-amendment · ◐ teilweise · ⚠ caveat.
Movement column = how the verdict moved 06-03 → 06-04 → **#3 (this)**.

| Crit | 06-03 | 06-04 | **#3 (live)** | Movement / diff | Live evidence (re-derived) |
|---|---|---|---|---|---|
| **C1** Core/RWDR pack separation | ✅ | ◐ HIGH | **✅** | ↓ then ↑; #3 confirms re-closure | No live `== "rwdr"` in core 8 files [E]; only motion-label `case_workspace.py:2229`; enforcer green-locally `[NV-CI]` |
| **C2** DomainPack interface | ✅* | ✅* | **✅\*** | stable | `domain_pack.py:24`, `seal_packs.py:65` [E]; registry-later per A-C2 |
| **C3** SealTypeImpact classifier | ✅ | ✅ | **✅** | stable | `orchestrator.py:315→333 normalize_seal_type()→pack_for` [E] |
| **C4** domain `required_fields()` | ✅ | ✅ (Konf 9→8) | **✅** | conf ↓ | `seal_required_fields.py:19` relocated [E]; 06-03's `orchestrator.py:55` ref stale |
| **C5** cross-cutting vs domain knowledge | ✅ | ✅* | **✅\*** | stable | `rag_schema.py:57 pack_affinity`, retrieval-inert per A-C5 [E] |
| **C6** tenant scoping | ✅ | ✅ | **✅** | stable (residual note) | `dependencies.py:132`, `memory_core.py:64-70` [E]; `apply_mutation` trusts row tenant — defense-in-depth `[A]` |
| **C7** killer-flow live | ✅* | ✅* | **✅\*** | stable | per A-C7 amendment; **but amendment rationale is false** (§2.4) |
| **C8** mobile <1s + bad-photo | ✅ | ✅ | **✅ ⚠** | stable | `mobile_triage.py:166`, `PocketCockpit.tsx:56-69` [E]; real <1s = `[NV]` (no live perf trace re-run) |
| **C9** no speculative abstraction | ✅ | ✅ +LOW | **✅** | caveat closed | O-Ring calc relocated to `oring_calc.py` (P1-4) [E]; no scaffolds |
| **C10** manufacturer feedback structured | ✅ | ✅ +LOW (echo dead) | **✅** | **echo now LIVE** | caller `rwdr_mvp_brief.py:2231`→`:2247`→`:4060`, L1-guarded `:2884` [E] (wired PR #84, 06-05) |
| **S1** Trace-Felder | ✅ | ✅ | **✅** | stable (06-03 matrix was stale) | `turn_timing.py:21-56`, `sse_contract.py:103-114` [E] |
| **S2** Tier-Disziplin | ✅ | ✅ LOW | **✅ ⚠** | caveat: see Risk #3 | guard wired `rag_orchestrator.py:996-998` [E]; **BM25 fallback bypass** (Scope B B1) |
| **S3** state-gate single-writer | ✅ | ◐ LOW | **✅** | ↓ then ↑ | enforcer now catches `model_copy` on governed receivers [E] `test_single_writer_invariant.py:74-80`; `produce_*` helpers `reducers.py:1375-1387` |
| **S4** RFQ-readiness | ✅ | ✅ (Konf 10→9) | **✅** | conf ↓ | `rfq_one_pager.py:21-33`, `evaluate_rfq_readiness()` [E] |
| **S5** mode-coverage | ◐ | ◐ LOW | **◐ (documented-open)** | stable | `contracts.py:15-27` 11 routes vs §5 modes [E]; deliberate divergence, parked |

**Verdict-movements that audit #3 confirms:** C1 (✅→◐→✅, re-closed by P1-4), S3
(✅→◐→✅, enforcer added), C9/C10 caveats closed, C10 echo wired live. **All AC1–10
met or met-per-amendment; S1–S4 met; S5 documented-open.**

### 1.x The three deliberately-open ledger items (DOCUMENTED-OPEN, not findings)

- (a) **Service-Account-Wiring** — `GOVERNANCE_LOG.md:30` "documented, **undated**" —
  weakest-documented (single line, no task spec) `[A]`.
- (b) **S5 Mode-Konsolidierung** — `GOVERNANCE_LOG.md:31` "[LOW, deliberate]" [E].
- (c) **`.env` `KEYCLOAK_ADMIN_PASSWORD` placeholder** — `GOVERNANCE_LOG.md:32,57-59`,
  runbook `KEYCLOAK_TENANT_ID_MAPPER.md:211-217` — well-documented owner action [E].

## 2. Amendments — exist / dated / justified / congruent?

| Amendment | Exists | Dated | Justified | Congruent w/ code | Source |
|---|---|---|---|---|---|
| **A-C7** envelope: drop `case_understanding_patch`/`rfq_brief_patch` stubs | ✅ | ✅ 06-04 | ✅ | **PARTIAL — see 2.4** | `..._v1_6_..._blueprint.md:1749-1759`; `GOVERNANCE_LOG.md:80-81` |
| **A-C2/C4** protocol-now / registry-later; orchestrator≠brief | ✅ | ✅ 06-04 | ✅ | ✅ (quantified "6≠31" only in 06-03 notes) | `CORE_PACK_BOUNDARY.md:5/30/46` |
| **A-499** `risk_readiness.py:499` kept as deliberate CORE check | ✅ | ✅ 06-04 | ✅ | ✅ (doc cites `:527/:555`, live `:533/:561` — line drift) | `CORE_PACK_BOUNDARY.md:89-95`; `GOVERNANCE_LOG.md:298-312` |
| **A-C5** `pack_affinity` marker-now / routing-later | ✅ | ✅ 06-04T14:43Z | ✅ | ✅ not in filter keys | `GOVERNANCE_LOG.md:250-294` |

**2.4 — Amendment incongruence (finding):** A-C7 was written into the **on-branch
V1.6** blueprint (§11 Multi-Output) but the canonical AC7 lives off-branch; the
off-branch **§6.4 still lists the removed stubs** (`AssistantTurnEnvelope = … +
CaseUnderstandingPatch + RFQBriefPatch + …`). A-C7's justification asserts "§6.4
does not exist as a heading" — **false**: off-branch §6.4 exists (line 418). The
relaxed bar is documented & dated, but its congruence-with-source claim is factually
wrong. `[E]` off-branch `§6.4`; re-run flagged "unrekonziliert" (`2026-06-04:217`).
**Severity: LOW** (doc-truth, not runtime), **fix-direction:** correct the amendment
text or reconcile/retire the off-branch §6.4 stub list. (Realistic.)

## 3. SCOPE B — adversarial enforcer-gap table

| # | Gap | Realistic / Theoretical | Severity | Fix-direction |
|---|---|---|---|---|
| B1 | **Tier-0 guard bypassed by BM25 Tier-2 fallback:** `hybrid_retrieve` raises `TierViolation`; `real_rag.py:121-128` swallows it → cascades to `bm25_repo.search` (`:173-181`) which has **no tier check** | Realistic | **HIGH** | Move the guard above the funnel (gate `real_rag` entry / `bm25_repo.search`), or re-raise `TierViolation` past the `except` |
| B2 | Guard is **not the sole funnel:** `_bm25_search`/`_qdrant_search_with_retry`/`bm25_repo.search` are unguarded primitives; doctrine claims "single guard at the funnel" (`turn_tier.py:9`) | Realistic | **HIGH** | Guard the primitives or assert single-entry invariant in a test |
| B3 | **Seal-type enforcer scans only 8 hardcoded files** (`test_core_seal_type_branching.py:42-51`); `frontend/`, `services/`, `mcp/`, `communication/`, most of `graph/`, all `domain/` are unscanned | Realistic | **HIGH** | Convert to deny-by-default walk over core packages; add a TS lint for `.tsx` |
| B4 | Seal-type enforcer blind to: dispatch-dict w/ scalar/dict values, `getattr`/`setattr`, `Subscript` config lookup, registry/polymorphism, `.format`, named-constant sets, tokens outside the 14-set, frontend TS branches (`ParameterWorkspaceTab.tsx:657`, `DecisionUnderstandingPanel.tsx:271`) | Realistic | MED | Add AST visitors for Subscript/Call/getattr; broaden token policy or doc the intent |
| B5 | Single-writer blind to: `.copy(update=)`, dict-mutation `state["governance"]=`, `setattr`, `deepcopy`+mutate, aliased constructor, renamed-local `model_copy`, modules outside `backend/app`, `tests`-named non-test dirs | Realistic | MED | Add `.copy`/Subscript/`setattr` detection; restrict the tests-exclusion to true test files |
| B6 | Single-writer has **no self-trip test** (cannot prove the detector still fires after refactor) | Realistic | MED | Add a synthetic-violation self-test (Target-1 has one; Target-2 doesn't) |
| B7 | Tier not declared on non-pre-gate routes (RFQ early-returns `dispatch.py:744-789`), MCP entry, and no `clear_declared_tier()` in product runtime → tier=None defaults to **allowed** (fail-open) or stale tier leaks across turns | Realistic | MED | Declare tier at every retrieval-capable entry; clear contextvar per turn |
| B8 | Doctrine denylist is **material-list-bound + German-only + lexical** (self-described "leaky", `output_guard.py:77`); "AFLAS ist NBR überlegen", English, or markdown-fragmented text escapes | Realistic | MED | Keep prompt+deterministic passthrough primary; widen token set; treat denylist as backstop only (already documented) |
| B9 | **C10 echo guarded by L1 only** (misses plural "sind geeignet" `final_guard.py:28-33`); L2 comparatives don't run on the technical final path (`final_guard.py:165` uses `_FORBIDDEN_PATTERNS` w/o comparatives) | Realistic | MED | Route echo label through L2 too; add comparative patterns to technical `_FORBIDDEN_PATTERNS` |
| B10 | Seal-type allowlist stale-guard keys on normalized **source text**; a semantic change with byte-identical text satisfies the guard | Realistic | LOW | Key on AST node identity / add owner-review marker |

**What IS solid (proven for its stated scope):** four repros block at L1 (regex-run)
[E]; L1/L2 share ONE comparative source via `comparative_ranking_patterns()`
(`output_guard.py:177-185` → `final_guard.py:9,97`) — no drift; Tier-0 guard is
genuinely fail-closed/default-on/bypass-logged at `hybrid_retrieve` with RED/GREEN
tests; seal-type enforcer has a self-trip + stale-allowlist test and 5
owner-documented allowlist entries; single-writer closes the direct/attribute
`model_copy` hole.

## 4. SCOPE C — Repo-Professionalität

| Area | Finding | Evidence | Severity |
|---|---|---|---|
| Secrets | **Committed Keycloak client-secret** (real 32-char value, tracked) | `client.json:13` [E] | **HIGH** |
| CI coverage | **No CI workflow runs pytest / architecture enforcers / doctrine suite** — only ripgrep v2-guard + `npm build` + BFF smoke + `ruff format --check` | `.github/workflows/langgraph-v2-guardrails.yml:23-66` [E]; `grep pytest .github/workflows` empty [E] | **HIGH** |
| Branch protection | **demo (all-PRs-target) is UNPROTECTED** (404); main `enforce_admins:false` (owner can bypass), 0 required reviews | `gh api .../branches/demo.../protection`→404; `.../main/protection` [E] | MED |
| CI config | **Dead auto-trigger:** `build-and-push.yml:10` triggers on removed `"CI"` workflow → auto build→deploy chain can't fire; header `:3` "triggered automatically" is now false | `build-and-push.yml:8-12`; ci.yml removed `abce3634` [E] | MED |
| Doc-truth | `.claude/settings.json:3` header still says "PROPOSED … restart to activate" — contradicts `ops.md:79-82` "hot-reload, no restart, verified/activated" | [E] both | LOW |
| Doc-truth | Tier-0 kill-switch doc, deploy-ledger doctrine, gate command-parsing doc all **ACCURATE** (re-verified vs code) | `ops.md:84-98` vs `turn_tier.py`; `ops.md:53-57` vs ledger [E] | — (clean) |
| Hygiene | `.git-blame-ignore-revs` **missing** `8a15bf1e` (C10-echo ruff-format commit); bulk commits correctly listed | [E] `grep 8a15bf1e` = 0 | LOW |
| Hygiene | Untracked legacy dirs linger on disk: `archive/`, `langgraph_backup/` (removed from git, not in `.gitignore`); `langchain_core_stub/` is a tracked likely-dead stub | `git ls-files` [E] | LOW/cosmetic |
| Hygiene (clean) | `langgraph_v2/` fully removed [E]; deps fully pinned (backend 73×`==`, frontend lockfile committed); `.gitignore` covers `.env*`/gate-logs/`_archive`; no other secret patterns | [E] | — |
| Branches | 23 remote (not ~80): **9 merged-deletable** (`ahead:0`), 13 stale-unmerged (139–1097 behind), 1 active demo; 54 local | `gh api .../branches` + compare [E] | LOW |
| CI runs | c10 failure pair (05:33) was transient ruff-format, **resolved** at 05:34; main & demo currently **all green** | `gh run list` [E] | — (clean) |
| Tolerated red | `workspaceMapping.test.tsx` genuinely fails (1/199), is the **only** red, no skip/xfail markers — accurately documented | `npx vitest run`; `frontend/src/test/workspaceMapping.test.tsx:393`; `ops.md:48-50` [E] | — (documented) |
| Ledger | **Both** live deploys logged with all 4 required elements (pinned `@sha256`, daemon rollback target, gate result, smoke); GHCR digest existence `[NV]` (no registry pull) | `GOVERNANCE_LOG.md:9-24,151-170` [E] | — (clean) |

## 5. Top-5 risks

1. **Committed Keycloak client-secret** (`client.json:13`) — rotate + untrack + add a secret-scan guard. HIGH.
2. **CI doesn't execute the "executable contracts."** The enforcers/doctrine suite/pytest that underpin every C1/S3/C9/doctrine "closed" claim are not run on push/PR; branch protection requires only `agent-bff-guardrails` (build+smoke). A regression merges green. HIGH. *(New in #3 — neither prior audit flagged it.)*
3. **Tier-0 retrieval guard is bypassable** (B1/B2): its own `TierViolation` routes a Tier-0 turn into the unguarded BM25 fallback; the "single funnel" claim is false. HIGH.
4. **Seal-type enforcer governs only 8 files** (B3) — any new core seal-type logic outside that closed list (incl. all frontend TS) is ungoverned. HIGH.
5. **demo is unprotected** + dead CI auto-trigger + amendment A-C7's false §6.4 claim — governance leans on owner discipline, not mechanical enforcement. MED.

## 6. Gesamtverdikt

**V1.7 exakt umgesetzt: ja-mit-Amendments.** At the live digest every AC1–10 is met
or met-per-documented-amendment and S1–S4 are met (C1/S3 re-closed by P1-4, C10 echo
wired by PR #84, deployed==merged proven); S5 is deliberately parked. The "mit-
Amendments" qualifier is binding because (a) the AC bar itself lives only off-branch
and is not the contract `AGENTS.md` enforces, and (b) amendment A-C7 carries a
factually-wrong justification about its own source (§6.4) — real but correctable.

**Repo professionell: mit-Auflagen.** Fundamentals are strong — pinned deps, an
accurate and complete deploy ledger, deployed==merged, a protected main, accurate
doctrine docs, and a single, openly-documented red test. But three conditions gate a
clean "ja": a real client-secret is committed, CI does not run the very enforcer/
doctrine tests the governance calls "executable contracts," and the all-PRs-target
demo branch has no protection — so today's green history rests on discipline, not
mechanism.

---

## Diff summary vs prior audits

- **vs 06-03:** C1 ✅→✅ (held, but 06-03 was pre-P1-4 over-claim); S3 ✅→✅;
  added new findings the 06-03 pass did not raise: CI-doesn't-run-enforcers (Risk #2),
  Tier-0 BM25 bypass (B1), committed client-secret, demo-unprotected, dead CI trigger.
- **vs 06-04 rerun:** C1 ◐→✅ and S3 ◐→✅ (re-run downgrades now re-closed by P1-4 and
  independently re-verified on live code); C10 echo dead→live (PR #84); the re-run's
  "nein → ja-mit-Amendments" nachtrag is confirmed (`ja-mit-Amendments`), with the
  new caveat that A-C7's source-congruence claim is factually wrong (§2.4).

## Verification notes

- Saved verbatim from the owner-approved draft; produced read-only, no fixes applied.
- Optional follow-up (owner request): a live PASS snapshot of the focused enforcer +
  doctrine suites would empirically sharpen Risk #2 (they pass locally yet never run
  in CI). Kept out of this static pass per the "statisch" mandate.
