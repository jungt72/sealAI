# IST-AUDIT V2.1 — Read-Only Deep-Dive (Repo vs. Konzept)

Auftrag: `scratch/IST_AUDIT_BRIEF.md`. Strikt read-only. Stand: 2026-06-22.
Branch: `feat/v2-cockpit-resizable`. Worktree: clean (vor diesem Report).
Evidenz-Disziplin: jede IST-Aussage `pfad:Zeile`; Status-Tag genau einer von
`[VERIFIZIERT] | [DOC-BEHAUPTET] | [NICHT-GEFUNDEN]`. Bei Unsicherheit: „nicht verifizierbar".

---

## OUTPUT_MANIFEST

1. Schritt 0 — SOLL-Dokumente (gefunden + FEHLEND)
2. Schritt 1A — Die „neun Invarianten I1–I9": Bestandsaufnahme + tatsächliche Enforcer
3. Schritt 1B — Kernel↔Narration-Grenze + Enforcer
4. Schritt 1C — Gate/CI: was tatsächlich erzwungen wird
5. Schritt 1D — Kernel-Komponenten (calc-registry, binding, formulas, derived, leak_detector, matrix)
6. Schritt 1E — Memory/Pipeline-Kette (distiller → store → pipeline → bind_params)
7. Schritt 1F — V2.1-Backlog / Arbeitsitems
8. Schritt 2 — Drift & Integritäts-Lücken
9. Schritt 3 — Repo-/Deploy-State
10. KERNBEFUNDE (max. 10)

---

## 1. Schritt 0 — SOLL-Dokumente

### Gefunden (mit Pfad)
- `docs/V2/sealingai_v2_1_produkt_konzept.md` — V2.1 WAS-SoT (Fall/Eingänge/Operationen/Kalibrierung). [VERIFIZIERT]
- `docs/V2/sealingai_v2_1_implementierungs_konzept_cc.md` — V2.1 WIE-SoT (Increments, §1.2 Self-Gates, §1.3 Owner-HALTs, §2 Inc-0-Audit-Pflicht). [VERIFIZIERT]
- `docs/V2/sealingai_v2_build_spec.md` — ausführbarer V2.0-Bauplan (§4 Verträge, §11 Grüne-Wiese-Grenze). [VERIFIZIERT]
- `docs/V2/sealingai_v2_architektur_prinzipien.md` — die tragenden Prinzipien (Vorrang-Anhang). [VERIFIZIERT]
- `docs/V2/sealingai_eval_seed_set_v0.md` — Abnahme-Lineal (Prosa). [VERIFIZIERT]
- `docs/V2/sealingai_system_prompt_l1.jinja` — L1-Generator-Seed (Doku-Kopie). [VERIFIZIERT]
- `docs/V2/OPTIMIZE_BACKLOG.md` — 6 nummerierte Optimierungs-Items. [VERIFIZIERT]
- Doktrin/Ops: `.claude/rules/{doctrine,ops,workflow,testing}.md`, `AGENTS.md`, `CLAUDE.md`. [VERIFIZIERT]
- Relay-Verträge: `ops/contracts/BUILDER_CONTRACT.md`, `ops/contracts/REVIEW_CONTRACT.md`. [VERIFIZIERT]
- Increment-Specs (Kernel-Härtung): `ops/increments/INC-{1,2,3a,3b}.md`. [VERIFIZIERT]
- `docs/ops/GOVERNANCE_LOG.md` (existiert, deploy-log). [VERIFIZIERT]
- V1-Invarianten-Katalog (anderes Thema, V1): `docs/audits/sealingai-engine/04_invariant_catalog.md` — nummeriert 1..N, betrifft Sealed-Output-Determinismus, NICHT die V2-Kernel-Doktrin. [VERIFIZIERT]

### FEHLEND / nicht-existent (Teil des IST)
- **Keine V2.1-„Charter"-Datei.** Repo-weite Suche nach `charter` trifft nur den Brief selbst (`scratch/IST_AUDIT_BRIEF.md`). [NICHT-GEFUNDEN]
- **Kein Dokument enumeriert „I1–I9".** Repo-weite Suche nach dem Token `I1-I9` / `I[1-9]`-Invarianten-Definitionen liefert NUR den Brief (`scratch/IST_AUDIT_BRIEF.md:22,28,31`). Im Code/Ops ist EINZIG **I5** benannt (`ops/contracts/REVIEW_CONTRACT.md:8`, `backend/tests/architecture/test_i5_narration_no_numbers.py:1`). [NICHT-GEFUNDEN]
- `ops/ESCALATION.md` — vom Builder-Contract referenziert (`ops/contracts/BUILDER_CONTRACT.md:21,28`), aktuell **nicht vorhanden** (= keine offene Eskalation; erwartetes Fehlen). [VERIFIZIERT]

**Folge für 1A:** Eine kanonische „I1–I9"-Liste lässt sich aus keinem Artefakt rekonstruieren (nur I5 ist verankert). 1A erfasst daher den IST der *tatsächlich existierenden* Enforcer und markiert die Nummerierung als ungeerdet.

---

## 2. Schritt 1A — Die „neun Invarianten I1–I9"

**SOLL (Brief):** „Die neun Invarianten I1-I9 — für jede einzeln: wird sie erzwungen, WO, WIE?"
(`scratch/IST_AUDIT_BRIEF.md:28`).

**IST der Nummerierung:** `[NICHT-GEFUNDEN]`. Es gibt keine I1..I9-Definition. `I5` = „kein Narrations-Pfad berührt Zahl/Limit/Toleranz/Selektion (Kernel-Doktrin)" (`ops/contracts/REVIEW_CONTRACT.md:8`) und „kein Narrations-Pfad erfindet eine Zahl" (`backend/tests/architecture/test_i5_narration_no_numbers.py:1-9`). I1–I4, I6–I9 sind **nirgends** belegt. Eine Zuordnung „I1=…, I2=…" wäre Erfindung → fail-closed nicht geliefert.

**IST der tatsächlichen Enforcer** (jeweils Status + Enforcer-Pfad):

| Invariante (faktisch im Code) | Status | Enforcer (`file:line`) |
|---|---|---|
| I5 — keine Engineering-Zahl im Narrations-Layer | PARTIAL | `backend/tests/architecture/test_i5_narration_no_numbers.py:16-35`. Scannt NUR 5 hartkodierte Dateien (`l1_generator.py`, `l3_verifier.py`, `prompts/assembler.py`, `llm/factory.py`, `llm/client.py`), nur **Float-Literale** (Integer ignoriert, `:30`), **.jinja nicht gescannt** (`:8`). |
| Import-Boundary `sealai_v2 ↔ app` (beidseitig) | ENFORCED | `backend/tests/architecture/test_v2_import_boundary.py:54,71` (AST, beide Richtungen + Synthetik-Gegenprobe `:102`). |
| Untrusted-Quarantäne (Fremdtext = Daten, nie Grounding) | ENFORCED | `backend/sealai_v2/tests/test_untrusted_quarantine.py:46,50` — `GroundingFact`/`Claim`-Ctor nur in Grounding-Lanes (`:22-25`); Seam-Verhalten `:54-67`. |
| Tenant-Scope P0 (server-seitig, fail-closed) | ENFORCED | `backend/sealai_v2/security/tenant.py` via `require_tenant` an Pipeline-Eintritt `backend/sealai_v2/pipeline/pipeline.py:126` und Matrix-Query `backend/sealai_v2/knowledge/matrix.py:146`. |
| Kernel = einzige Zahl-Quelle / fail-closed Binding | ENFORCED (Code) | `backend/sealai_v2/core/calc/binding.py:238-294` (deklarierte Tabelle, fail-closed); kernel-computed nie Eingabe `:252`. Laufzeit-Wirkung, kein eigener AST-Test. |
| Reviewed-only Korrektur (L3 erfindet keine Wahrheit) | ENFORCED (Code) | `backend/sealai_v2/core/l3_verifier.py` (Korrektur nur aus `reviewed`-Eintrag, sonst deterministischer Hedge) — Doktrin `.claude/rules/doctrine.md`. |
| Zirkularitäts-Guard Wissens-Stores (kein LLM erdet LLM) | ENFORCED (Loader) | calc: `backend/sealai_v2/knowledge/calc_registry.py:138-153`; matrix: `backend/sealai_v2/knowledge/matrix.py:93-98`. |
| Single-Writer / Seal-Type-Branching | ENFORCED, aber **V1-skopiert** | `backend/tests/architecture/test_single_writer_invariant.py` (S3, `backend/app/agent/state`), `…/test_core_seal_type_branching.py` (governed core `app/`). Betreffen NICHT `sealai_v2`. |

**Befund:** Von 5 Architektur-Enforcer-Tests im Glob sind nur **2** V2-relevant (Import-Boundary, I5-Narration). 3 (`test_ssot_guardrails.py`, `test_single_writer_invariant.py`, `test_core_seal_type_branching.py`) erzwingen den **V1**-`app/`-Baum. [VERIFIZIERT: `backend/tests/architecture/test_ssot_guardrails.py:13`, `…test_single_writer_invariant.py:1-3`, `…test_core_seal_type_branching.py:1-8`]

---

## 3. Schritt 1B — Kernel↔Narration-Grenze

**SOLL:** Berechnungen + geerdete Fakten + Artefakt-Rendering = Code; Schließen/Gesprächsantwort = LLM; Provenienz sichtbar (`docs/V2/sealingai_v2_build_spec.md:44-46`, §4 Komponenten-Verträge). „Backend besitzt Fakten/Zahlen, LLM erzählt nur" (`docs/V2/sealingai_v2_1_implementierungs_konzept_cc.md:25`).

**IST der Grenze im Code:** [VERIFIZIERT]
- `core/calc/` = reine Domänen-/Rechenlogik, kein I/O, kein LLM (Modul-Docstrings: `formulas.py:1-7`, `binding.py:33`, `evaluator.py:6`, `derived.py:11`, `leak_detector.py:48`).
- Narration: `core/l1_generator.py` (reine Orchestrierung, Client = einziges I/O, `:1-6`); LLM-Output wird durch `strip_sourcing()` gefiltert, BEVOR zurückgegeben (`core/l1_generator.py:91`).
- Safety-Framing single-source im Code, nicht im LLM: `core/framing.py:17-40` (CLAIM_BOUNDARY/VORLAEUFIG/GELTUNGSRAHMEN).

**IST der Enforcer der Grenze:**
- **Import-Boundary** (`backend/tests/architecture/test_v2_import_boundary.py:54,71`) — ENFORCED. [VERIFIZIERT]
- **I5-Scanner** (`backend/tests/architecture/test_i5_narration_no_numbers.py`) — PARTIAL: floats-only, 5 Dateien, kein Jinja (siehe 1A). [VERIFIZIERT]
- **Untrusted-Quarantäne** (`backend/sealai_v2/tests/test_untrusted_quarantine.py:46-67`) — ENFORCED; untrusted erreicht den Prompt nur als delimitierte DATEN, nie als „Belegte Fakten". [VERIFIZIERT]
- **Parametric-Leak-Detektor** (`core/calc/leak_detector.py:145`) — deterministische Enforcement-Schicht hinter dem L1-Prompt; verdrahtet in L3 auf Draft/Regeneration/Hedge (`core/l3_verifier.py:727,791,842`) UND als Eval-AGENT-FINAL-Schranke (`eval/harness.py:153`, `eval/multiturn.py:257`). [VERIFIZIERT]
- **sourcing_guard** (`core/sourcing_guard.py:35`) — entfernt RFQ-/Beschaffungssätze aus L1; konservativ, im Zweifel nicht strippen (`:36-46`). Kein eigener Architektur-Test; Akzeptanz über Unit-Tests (INC-2). [VERIFIZIERT]

**DELTA:** Grenze im Code intakt; Enforcer vorhanden, aber **uneinheitlich** — der härteste mechanische Schutz (I5-Scanner) ist scope-begrenzt (floats/5-Dateien/kein-Jinja). Der substanzielle Schutz gegen erfundene Engineering-Zahlen ruht auf dem Laufzeit-`leak_detector` (regex/heuristisch, dokumentierte Residuen `leak_detector.py:33-46`), nicht auf einem statischen Gate. [VERIFIZIERT]

---

## 4. Schritt 1C — Gate/CI

**SOLL:** Eval-REPLAY ist das Abnahme-Lineal; 8 harte Schranken halten **1.000** durch *jedes* Increment (`docs/V2/sealingai_v2_1_implementierungs_konzept_cc.md:32`, §1.2). HALT + Eval-REPLAY je Meilenstein (`docs/V2/sealingai_v2_build_spec.md:176`).

**IST pre-merge Gate** (`ops/gate.sh`): 5 Stufen. [VERIFIZIERT]
1. `ruff format --check backend/`, Pin 0.6.9 (`ops/gate.sh:14-16`).
2. Import-Boundary-Keystone (`:18-19`).
3. Architektur-Enforcer-Glob `backend/tests/architecture` (`:21-22`) — enthält I5 + die 3 V1-Tests.
4. V2-Offline-Suite `backend/sealai_v2` (`:24-25`).
5. Eval↔Tree-Bindung (`:27-32`) — **WARN, kein BLOCK** pre-merge; blockt nur das Deploy-Gate (`ops/contracts/REVIEW_CONTRACT.md:9-12`).

**IST CI** (`.github/workflows/`): [VERIFIZIERT]
- `backend-contracts.yml:45` Architektur-Glob (→ I5 + Import-Boundary laufen in CI); `:53` Import-Boundary explizit; `:64-72` V1-Doktrin-Fast-Suite.
- `v2-contracts.yml:33` Import-Boundary; `:44` V2-Offline-Suite (enthält Quarantäne-, Leak-, Calc-, Memory-Tests).
- `langgraph-v2-guardrails.yml:55-66` `ruff format --check backend/` (informativ; chronisch rot aus V1-Basis-Debt, nicht-blockierend).

**DELTA (Diskrepanzen):**
- **Eval-REPLAY / die 8 Schranken laufen in KEINEM CI-Workflow.** Kein `.github/workflows/*` ruft `python -m sealai_v2.eval` oder `ops/gate.sh --bind-eval`. Die „Schranken 1.000 durch jedes Increment"-Garantie ist NUR lokal/owner-adjudiziert, nicht maschinell pre-merge. [VERIFIZIERT — Negativ-Suche über `.github/workflows/`]
- **`ops/gate.sh` wird von keinem CI-Workflow aufgerufen** (seine Bestandteile laufen einzeln in CI; Stufe-5-Eval-Bindung läuft nirgends in CI). [VERIFIZIERT]
- I5-Scanner-Scope (floats/5-Dateien/kein-Jinja) ist die in 1A/1B genannte CI-wirksame Lücke. [VERIFIZIERT]

---

## 5. Schritt 1D — Kernel-Komponenten

Alle Pfade `backend/sealai_v2/`. Status je: vorhanden / verdrahtet / deterministisch.

- **calc-registry** — `knowledge/calc_registry.py`. Formel = reviewte CODE, per id aus `core/calc/formulas.FORMULAS` gebunden, **nie ge-eval()'t, nie LLM-abgeleitet** (`:1-11`). Reviewed-Def erzwingt source+provenance+validity+gebundene impl (`:138-153`). Daten: `knowledge/calc_seed.json` = 3 reviewte Defs (`umfangsgeschwindigkeit`, `pv_wert`, `verpressung_prozent`). [VERIFIZIERT]
- **formulas** — `core/calc/formulas.py:14,20,25`: 3 reine Funktionen; `FORMULAS`-Map `:34-38`. Deterministisch. [VERIFIZIERT]
- **binding** — `core/calc/binding.py:238` `bind_params`, deklarierte Tabelle `_BINDINGS:133-141` (wellendurchmesser→d1_mm, drehzahl→rpm, druck→p_bar, geschwindigkeit→v_m_s). Fail-closed: normalize→exact-synonym-lookup (`:55-75`), Konflikt-Erkennung (`:259-269`), kernel_computed nie Eingabe (`:252`). Deterministisch. [VERIFIZIERT]
- **derived** — `core/calc/derived.py:55` `recompute_derived`: bindet aktuelle Inputs → evaluiert → mappt jeden Wert auf `DerivedFact(provenance="kernel_computed")` mit Parent-Refs (`:76-90`). Immer Voll-Recompute (kein stale). Rein. [VERIFIZIERT]
- **leak_detector** — `core/calc/leak_detector.py:145` `detect_parametric_leaks`. Feuert nur bei Assertion-Signatur (`:71`), Quantity-Gate, mit Kern-Toleranz-Exempt (`:120-128`). Rein, kein I/O. Verdrahtet L3 + Eval (siehe 1B). [VERIFIZIERT]
- **matrix** — `knowledge/matrix.py:130` `InProcessCompatibilityMatrix`, Zirkularitäts-Guard `:93-98`, verdict-only (kein Steuerlogik, `:17-20`), tenant-scoped `:146`. Daten: `knowledge/matrix_seed.json` = **27 Zellen** (= Konzept-Soll, `produkt_konzept.md:193`). Deterministische Scope-Tag-Overlap (≥2 Hits). [VERIFIZIERT]
- **evaluator** (Kontext) — `core/calc/evaluator.py:35` `CascadeCalcEngine`: topologischer Fixpunkt, fail-closed bei fehlenden Inputs/außerhalb Gültigkeit/Rechenfehler (`:96-123`); Limit→fact-only-Warnung ohne Zahl/Material-Richtung (`:132-145`). [VERIFIZIERT]
- **inline_extract** (INC-3a/3b) — `core/calc/inline_extract.py` rein, verdrahtet vor `bind_params` (`pipeline.py:205-207`). [VERIFIZIERT]

**Weitere Seed-Stores:** `knowledge/fachkarten_seed.json` = 9 Karten; `knowledge/trap_catalog.json` = 18 reviewed + 2 draft; `knowledge/archetypes_seed.json` = Profile `getriebe`, `ruehrwerk`. [VERIFIZIERT]

---

## 6. Schritt 1E — Memory/Pipeline-Kette

**Tatsächlicher Fluss** (`backend/sealai_v2/pipeline/pipeline.py`, `run`): [VERIFIZIERT]
`flush_memory` (Ordering-Guard, `:138-142`) → `recall` (`:145-152`) → typisierter `Case.from_case_state` + Projektion (`:160-163`) → `understand` nebenläufig (`:171-185`) → `ground` (`:188-201`) → **compute**: `bind_params(merge_inline(mem.case_state, extract_inline(question)))` (`:205-207`) → `generate` L1 (`:238-251`) → `verify` L3 (`:257-276`) → `cite` (stub, `:278-279`) → `remember`/distill (`:289-313`, Hintergrund `:417/434`).

**distiller → store → pipeline → bind_params (Stand):** [VERIFIZIERT]
- `memory/distiller.py` — konservativ, extrahiert nur user-genannte Fakten (`:1-10`); numeric-trace-Guard droppt Fakten, deren Zahlen nicht im User-Text vorkommen (`:84-102`); fail-safe Parse (`:104-116`).
- `memory/integrity.py:20,31,42` — `numerics()`/`untraceable_numeric_facts()`/`memory_integrity_clean()`, geteilt von Distiller-Guard UND Eval-Schranke `memory_fabrication` (`:1-7`).
- `memory/store.py` — `InProcessConversationMemory`/`InProcessCrossSessionMemory`; durable SQLAlchemy-Adapter (`db/*`) bei gesetzter `database_url` (`pipeline.py:542-558`).
- `core/calc/binding.py` — Endpunkt: L4-durable-Fakten ausgeschlossen, Inline-Overlay frisch>recalled (`pipeline.py:205-207`).
- This-Session-Case-State (L2) und Cross-Session-Durable (L4) sind getrennt; durable speist NIE den Calc-Binder (`pipeline.py:153-156`). [VERIFIZIERT]

**Befund:** Kette vollständig verdrahtet (M5/M8 + INC-3a/3b). Memory ist inert ohne Session (Eval = Single-Turn, byte-identischer No-Op, `pipeline.py:533-538`). [VERIFIZIERT]

---

## 7. Schritt 1F — V2.1-Backlog / Arbeitsitems

**Zwei getrennte Increment-Nummerierungen** (nicht verwechseln): [VERIFIZIERT]
- (a) V2.1-**Produkt**-Increments (`implementierungs_konzept_cc.md:86-119`): Inc 0 Audit, Inc 1 Case+Archetyp, Inc 2 Kalibrierung, Inc 3 Decode/Diagnose, Inc 4 Gegencheck, Inc 5 Alternativen.
- (b) `ops/increments/INC-*` **Kernel-Härtung** (Relay-Items): INC-1 Smoke, INC-2 sourcing_guard, INC-3a/3b inline-extractor.

**Status (a) Produkt-Increments:**
- Inc 0 (read-only Audit) — DIESER Report. Owner-HALT offen. [VERIFIZIERT]
- Inc 1 (Case + Archetyp + archetyp-geführtes Verstehen) — **im Code bereits substanziell vorhanden**, obwohl der Konzept-Text ihn als „ZUERST, morgen" rahmt (`implementierungs_konzept_cc.md:86`): typisierter `Case.from_case_state` (`pipeline.py:160`), Archetyp-Store geladen (`pipeline.py:571`), `_archetype_context` injiziert Interview-Fragen/blinde-Flecken (`pipeline.py:347-363`), Profile `getriebe`+`ruehrwerk` geseedet (= Konzept-Starter-Satz, `produkt_konzept.md:234,237`). [VERIFIZIERT] → siehe Drift §8.
- Inc 2–5 — keine Belege im Code (Kalibrierungs-Prompt-Arbeit, Decode/Diagnose/Gegencheck/Alternativen). [NICHT-GEFUNDEN]

**Status (b) INC-Items:** alle drei geliefert (git: `3bfc9002` INC-3b, `378deb61` INC-3a, `102443fa` INC-2; `00da7d0c` I5-Scanner, `55e45b40` Anti-Spin). [VERIFIZIERT]

**Status `OPTIMIZE_BACKLOG.md` (6 Items, keiner cutover-/pilot-gating):** [VERIFIZIERT]
- #1 Live-Token-Streaming — offen/deferred (`:1-25`).
- #2 Grounding/Citation-Präzision (zitierte Fachkarte muss Claim-Subjekt treffen) — offen (`:27-44`).
- #3 CALC-SYMBOL-LAG-01 must_compute relax — offen, „fold into next eval-seed touch" (`:46-61`).
- #4 Life-Number-Doktrin-Linie — konsolidiert in #6 (`:63-80`).
- #5 L3-Over-Fire (CALC-MEM-01, stochastisch ~29 %, auf jeden reviewed-trap-Hedge erweitert) — offen, **#1 Fast-Follow** (`:82-108`).
- #6 L1 Lebensdauer-Range — **✅ RESOLVED 2026-06-17** (`:110-128`).

---

## 8. Schritt 2 — Drift & Integritäts-Lücken

1. **Charter-/Invarianten-Drift.** Brief + Doktrin sprechen von „I1–I9" als kanonischem Set; es existiert **kein** definierendes Dokument (nur I5 verankert). Eine Charter-Datei fehlt. [NICHT-GEFUNDEN — `scratch/IST_AUDIT_BRIEF.md:22,28`; nur `ops/contracts/REVIEW_CONTRACT.md:8`]
2. **Konzept-vs-Code-Drift bei V2.1 Inc 1.** Das WIE-Konzept (datiert, `:86`) präsentiert Inc 0 als Pflicht-Erststep „bevor eine Zeile entsteht" (`:50`) und Inc 1 als „morgen" — der Code enthält bereits den typisierten `Case` + die geseedeten Archetyp-Profile + die Verdrahtung (`pipeline.py:160,347,571`). Code ist dem dokumentierten Gate-Ablauf **voraus**. [VERIFIZIERT]
3. **Enforcer behauptet, aber CI-inaktiv für den Kern-Maßstab.** Konzept: Schranken 1.000 durch jedes Increment (`implementierungs_konzept_cc.md:32`). Im CI läuft **kein** Eval-REPLAY; die Eval↔Tree-Bindung (`ops/gate.sh:27-32`) ist pre-merge nur WARN. Der eigentliche V2-Abnahme-Maßstab ist nicht maschinell pre-merge erzwungen. [VERIFIZIERT]
4. **Enforcer-Glob mischt V1/V2.** 3 von 5 Architektur-Tests im Glob (gate.sh:21, CI backend-contracts:45) erzwingen den V1-`app/`-Baum, nicht `sealai_v2` (`test_ssot_guardrails.py:13`, `test_single_writer_invariant.py:1`, `test_core_seal_type_branching.py:1`). Der V2-Kern-Narration-Schutz ruht auf 2 Tests. [VERIFIZIERT]
5. **I5 statisch nur partiell.** Float-only, 5 hartkodierte Dateien, **kein Jinja-Scan** (`test_i5_narration_no_numbers.py:8,16-30`) — eine erfundene Integer-Zahl oder eine Zahl in einem `.jinja`-Prompt würde der statische I5-Enforcer nicht fangen (Laufzeit-`leak_detector` ist die heuristische Ergänzung, nicht deckungsgleich). [VERIFIZIERT]
6. **Worktree-Realität ist ein V2.1-Audit auf einem Frontend-Branch.** Aktueller Branch `feat/v2-cockpit-resizable` (Cockpit-Layout), nicht ein `feat/v2.1-incN`-Branch. Der Audit läuft gegen diesen HEAD. [VERIFIZIERT]

---

## 9. Schritt 3 — Repo-/Deploy-State (nur Status)

**Branch + Divergenz:** [VERIFIZIERT]
- Aktiv: `feat/v2-cockpit-resizable`, Worktree clean.
- vs `origin/demo/rwdr-limited-external`: **ahead 2, behind 79**.
- vs `origin/main`: **ahead 13, behind 188**.
- Letzte Commits: `3bfc9002` (INC-3b) … `00da7d0c` (I5) … `de3d0528` (relay cleanup-on-fail).

**Deploy-Blocker (VORHANDEN / FEHLT / TEILWEISE):**
- **Base-Image-SHA-Pinning:** TEILWEISE verifizierbar. `ops/deploy-ledger.jsonl` belegt die Pinning-Praxis (letzter Eintrag: `run_label=kern-fix-01`, `image_sha=sha256:5b62e476…`, `rollback_from=sha256:10985276…`, `git_sha=53145401`, `dirty=true`, `ts=2026-06-20`). Der produktive Pin liegt in `.env.prod` — **gesperrt, nicht lesbar** → nicht voll verifizierbar. [TEILWEISE]
- **`ops/v2-flip.sh`-Routing (öffentlicher `/api/v2`-Flip):** VORHANDEN als Datei (Verzeichnis-Listing), aber **owner-gegated/gesperrt** — der PreToolUse-Hook `ops/hooks/relay-deny.sh` blockt jeden Zugriff darauf („owner-triggered only"; in dieser Sitzung zweimal ausgelöst). Status: vorhanden, gehalten. Doktrin: `implementierungs_konzept_cc.md:181`, `.claude/rules/ops.md`. [VERIFIZIERT/„gehalten"]
- **`feat → demo → main`-Reconvergence:** FEHLT. Branch ist behind 79 (demo) / 188 (main); nicht reconverged. [VERIFIZIERT]
- **Charter-Reconcile:** FEHLT (keine Charter-Datei existiert; siehe §1/§8). [NICHT-GEFUNDEN]
- **Letzter V2-Backend-Deploy:** `kern-fix-01` (2026-06-20, `dirty=true`), `ops/deploy-ledger.jsonl:1`. [VERIFIZIERT]

---

## 10. KERNBEFUNDE (max. 10)

1. **[NICHT-GEFUNDEN]** Kein Dokument definiert „I1–I9"; nur **I5** ist verankert (`ops/contracts/REVIEW_CONTRACT.md:8`, `backend/tests/architecture/test_i5_narration_no_numbers.py:1`). Die im Brief vorausgesetzte Invarianten-Charter existiert nicht.
2. **[NICHT-GEFUNDEN]** Keine V2.1-Charter-Datei im Repo (nur der Brief nennt „charter"); `ops/ESCALATION.md` ebenfalls abwesend (= keine offene Eskalation).
3. **[VERIFIZIERT]** Eval-REPLAY / die 8 Schranken laufen in **keinem** CI-Workflow; pre-merge ist die Eval↔Tree-Bindung nur WARN (`ops/gate.sh:27-32`). Der V2-Abnahme-Maßstab ist nicht maschinell pre-merge erzwungen.
4. **[VERIFIZIERT]** Von 5 Architektur-Enforcer-Tests im Glob sind **3 V1-skopiert** (`test_ssot_guardrails.py:13`, `test_single_writer_invariant.py:1`, `test_core_seal_type_branching.py:1`); der V2-Kernel-Narration-Schutz ruht auf nur 2 Tests (Import-Boundary + I5).
5. **[VERIFIZIERT]** Der statische I5-Enforcer ist scope-begrenzt: Float-only, 5 hartkodierte Dateien, **kein Jinja-Scan** (`test_i5_narration_no_numbers.py:8,16-30`). Substanzieller Zahl-Schutz ruht auf dem heuristischen Laufzeit-`leak_detector` (`core/calc/leak_detector.py:33-46` dokumentierte Residuen).
6. **[VERIFIZIERT]** V2.1-Produkt-Inc-1 (Case + Archetypen) ist **im Code bereits vorhanden** (`pipeline.py:160,347,571`; Profile getriebe/ruehrwerk), obwohl das WIE-Konzept ihn als „morgen, nach Inc-0-Audit" rahmt (`implementierungs_konzept_cc.md:50,86`) — Code dem dokumentierten Gate voraus.
7. **[VERIFIZIERT]** Kernel-Determinismus intakt: Formeln = gebundene reviewte CODE (`calc_registry.py:138-153`, `formulas.py:34`), Binding fail-closed (`binding.py:238-294`), Matrix circularity-guarded/verdict-only/27 Zellen (`matrix.py:93-98`), kernel_computed nie Eingabe (`binding.py:252`).
8. **[VERIFIZIERT]** Memory-Kette vollständig verdrahtet (recall→ground→bind→generate→verify→cite→distill, `pipeline.py:145-313`); numeric-trace-Guard im Distiller geteilt mit der Eval-Schranke `memory_fabrication` (`memory/integrity.py:1-7`, `distiller.py:84-102`); durable L4 speist nie den Calc-Binder (`pipeline.py:153-156`).
9. **[VERIFIZIERT]** Backlog: `OPTIMIZE_BACKLOG` #6 RESOLVED (2026-06-17), #5 (L3-Over-Fire, ~29 % stochastisch) offen als #1-Fast-Follow (`OPTIMIZE_BACKLOG.md:82-108`); #1–#4 offen/deferred, keiner cutover-gating. INC-1/2/3a/3b geliefert (git HEAD).
10. **[VERIFIZIERT]** Repo-State: `feat/v2-cockpit-resizable`, ahead 2/behind 79 (demo), ahead 13/behind 188 (main); Reconvergence FEHLT. Letzter V2-Deploy `kern-fix-01` (2026-06-20, dirty, `ops/deploy-ledger.jsonl:1`); `/api/v2`-Flip vorhanden aber owner-gehalten (`ops/hooks/relay-deny.sh`); Base-Image-Pin in `.env.prod` nicht lesbar → TEILWEISE.

---

*Ende. Reine IST-Erfassung — keine Empfehlungen, keine Fixes. Nicht verifizierbare Punkte sind als solche markiert (Base-Image-Pin: `.env.prod` gesperrt; I1–I4/I6–I9: kein Artefakt).*
