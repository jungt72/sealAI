# IST-AUDIT V2.1 — Read-Only Deep-Dive Ist-Abgleich (Code vs. Konzept)

Auftrag: `AUDIT-IST-0623` (Prompt). Strikt read-only — einzige Mutation ist diese Datei.
Stand: **2026-06-23**. Branch: **`main`**, HEAD **`5c67cfad`**. Worktree: clean bis auf **2 untracked
Dateien** (`backend/sealai_v2/core/seal_spec_extract.py`, `…/tests/test_seal_spec_extract.py`).
Vergleichsbasis: `docs/audits/v2/IST_2026-06-22.md` (Vorgänger, Branch `feat/v2-cockpit-resizable`) +
Sibling `docs/audits/v2/GAP_INC2-5_2026-06-22.md` (gegen `bbd8ebdc`).

**SOLL-Quellen:** `docs/V2/sealingai_v2_1_produkt_konzept.md` (WAS), `…_implementierungs_konzept_cc.md`
(WIE), `…_invarianten_charter.md` (Charter I1–I9/A0), `…_build_spec.md` (Seams).

**Evidenz-Disziplin:** jede IST-Aussage mit Code-`file:line` (auf `main` HEAD neu gelesen).
Status-Enum: `ERFÜLLT | TEILWEISE | OFFEN | ABWEICHUNG | DEFERRED | UNBELEGT`. **Fail-closed:** kein
Beleg → OFFEN/UNBELEGT, nie „ERFÜLLT" angenommen. Live-Eval nicht ausführbar (read-only/kein Key) →
Schranken aus committeten Lauf-Artefakten + GOVERNANCE_LOG zitiert. Keine Empfehlung, kein Fix.

---

## OUTPUT_MANIFEST
A Doktrin & Invarianten · B Pipeline & Case · C Wissens-Dimensionen 1–7 · D Eingänge & Operationen ·
E Trust-Spine L1–L4 · F Eval-Harness · G Kalibrierung (Inc-2) · H Deploy/Governance ·
Spezial-Probe „Matrix-Vokabular" · Increment-Status-Ledger · ABWEICHUNGEN · Delta vs IST_2026-06-22 ·
KERNBEFUNDE.

---

## A — Doktrin & Invarianten (Charter `sealingai_v2_invarianten_charter.md`; Produkt §3; Impl §1.1)

| Inv. | Aussage (Charter) | IST-Befund + Code-Beleg (`backend/sealai_v2/` bzw. `backend/tests/`) | Status |
|---|---|---|---|
| **I1/I2** | Kernel einzige Zahl-Quelle; computed nie Eingabe | `core/calc/binding.py:238-294` (`bind_params`, fail-closed, Konflikt :259-269), `:252` (`kernel_computed` continue → nie Input); `core/calc/derived.py:55` (`recompute_derived`), `:87` (`provenance="kernel_computed"`, Voll-Recompute) | IMPLEMENTIERT (Code, Laufzeit; kein Enforcer-Test) |
| **I3** | Import-Grenze `sealai_v2 ↔ app`, beidseitig | `backend/tests/architecture/test_v2_import_boundary.py:8-9` (beide Richtungen deklariert), `:54` `test_v2_does_not_import_app`, AST `:21,35` | ENFORCED |
| **I4** | Fremdtext = Daten, nie Grounding | `backend/sealai_v2/tests/test_untrusted_quarantine.py:46-51` (`GroundingFact`/`Claim`-Ctor nur in Grounding-Lanes; **`matrix.py` jetzt zugelassene Lane** `:23`), `:54-63` (untrusted → delimitierte DATEN), `:70` (No-Op byte-identisch) | ENFORCED |
| **I5** | Kein Narrations-Pfad erfindet Engineering-Zahl | `backend/tests/architecture/test_i5_narration_no_numbers.py`: Python-Float-Scan `:62-87` (5 Dateien `:19-25`, **float-only** `:67` — Integer ignoriert) **+ NEU Jinja-Scan (I5b)** `:44-54` (`_jinja_engineering_hits`) + Enforcement über `**/*.jinja` `:116-128`; Laufzeit `core/calc/leak_detector.py:145` (Residuen `:33-46`) | TEILWEISE (**Jinja jetzt gescannt** — s. ABWEICHUNG #1; Rest-Lücke = Integer in 5 py-Dateien) |
| **I6** | L3 korrigiert nur reviewed; erfindet keine Wahrheit | `core/l3_verifier.py:321-351` (`build_correction_note`, nur `reviewed` `:339`), `:354-391` (`build_hedge`), `:307-318` (`_scoped_fact` Topic-Scope), Doktrin-Docstring `:6-13` | IMPLEMENTIERT (Code, Laufzeit; kein Enforcer-Test) |
| **I7** | Zirkularitäts-Guard Wissens-Stores | `knowledge/calc_registry.py:138-141ff` (reviewed=sourced+provenanced+validated+bound), `matrix.py:93-98`, `archetypes.py:130-135`, `fachkarten.py:8-14` | ENFORCED (Loader) |
| **I8** | Tenant-Scope P0, server-seitig fail-closed | `security/tenant.py` via `require_tenant` @ `pipeline.py:126`, `matrix.py:146`; `contracts.py:153-175` (`AuthError`/`VerifiedIdentity` fail-closed) | ENFORCED |
| **I9** | Durable L4 speist nie Calc-Binder | `pipeline.py:153-156` (durable getrennt), `:205-207` (`bind_params(merge_inline(case_state, …))` — „L4 durable facts excluded") | IMPLEMENTIERT (Verdrahtung + Kommentar, kein Enforcer-Test) |
| **A0** | Eval-Schranken 1.000 hart am Deploy | `ops/v2_deploy_gate.py:25-30` (deterministische Schranken), `:33-84` (`find_gated_run`: tree_hash-Match + adjudication + alle Gates 1.0 + `n_units_pending==0`), `:94-99` (**exit 2** sonst) | ENFORCED AT DEPLOY (pre-merge `gate.sh` Stufe-5 = WARN by design) |

Doktrin-Querschnitt: Safety-Framing single-source im Code (`core/framing.py:17-40` CLAIM_BOUNDARY/
VORLAEUFIG/GELTUNGSRAHMEN), nicht im LLM; L1-Output durch `strip_sourcing()` gefiltert
(`core/l1_generator.py:91`); Jinja entscheidet keinen Domäneninhalt (Assembler rendert text+quelle).

> Benannte Charter-Durchsetzungslücken (`charter.md:32-37`): (1) I5-Scanner scope-begrenzt —
> **teil-aufgelöst** (Jinja jetzt gescannt; Integer-Rest bleibt); (2) Architektur-Glob mischt V1
> (`test_ssot_guardrails.py:13`, `test_single_writer_invariant.py:1`, `test_core_seal_type_branching.py:1`
> erzwingen `app/`, nicht `sealai_v2`) — **unverändert** (s. IST_2026-06-22 §1A/§8.4, nicht dupliziert).

---

## B — Pipeline & Case (Produkt §4; Impl §3/§5.1; build_spec §5)

- **Dünne lineare Kette** (verstehen→grounden→rechnen→antworten→verifizieren→zitieren→erinnern),
  kein Routing-Geflecht: `pipeline/pipeline.py` `run` — `recall :145-152` → typisierter
  `Case.from_case_state` + Projektion `:160-161` → `understand` **nebenläufig** `:171-185`, awaited
  `:235-237` → `ground :188-201` → `compute`/`bind_params :205-221` → `generate :238-251` →
  `verify :257-276` → `cite` (Stub) `:278-279` → `remember`/distill `:289-313`. **[ERFÜLLT]**
- **Soft-Intent annotate-only, kein Hard-Routing:** `core/contracts.py:97-105` (`Intent` „NEVER
  used to gate/route"), `pipeline/stages.py:69-71` + server-seitige Key-Validierung `:84-87`
  (LLM-erfundener Archetyp-Key überlebt nie), `pipeline.py:165-167,230-234`. **[ERFÜLLT]**
- **Tiefe-nach-Fragetyp in L1:** `prompts/system_l1.jinja:40-43` („kein Tiefe-Verbot … Substanz
  vs. Geschwätz"), Kalibrierungs-/Knapp-Regeln `:62-63`. **[ERFÜLLT]**
- **`Case` typisiert (§5.1):** `core/contracts.py:476-510` (`Case`, `from_case_state :501-505`,
  `to_prompt_context :507-510` byte-identisch). Slots `conditions/medium/geometry/seal_spec` sind
  **SCAFFOLD** `:490-499` („populated later via decode/describe adapters"); nur `archetype` real
  (G4). **[TEILWEISE]** — typisiertes Objekt vorhanden, vier Slots noch ungefüllt (Inc-1-konform).

---

## C — Wissens-Dimensionen 1–7 (Produkt §7 Tabelle :190-198)

| # | Dimension | IST + Beleg | Status |
|---|---|---|---|
| 1 | Fachkarten | `knowledge/fachkarten.py` (Loader+Guard `:8-14`, `reviewed_claims :71-72`) + `fachkarten_seed.json` (**9 Karten**, 37 reviewed Claim-States) | **ERFÜLLT** |
| 2 | Verträglichkeitsmatrix | `knowledge/matrix.py` (Guard `:93-98`, verdict-only `:17-20`, tenant `:146`) + `matrix_seed.json` (**27 Zellen**) | **ERFÜLLT** |
| 3 | Rechenkern | `knowledge/calc_registry.py:138ff` (reviewed-Guard) + `calc_seed.json` (**3 reviewte Defs**: umfangsgeschwindigkeit/pv_wert/verpressung_prozent), `core/calc/formulas.py` (gebundene reviewte CODE) | **ERFÜLLT** |
| 4 | Anwendungs-Archetypen | `knowledge/archetypes.py` (Schema `:38-71`, Loader `:139-154`, Guard `:130-135`) + `archetypes_seed.json` (**2 reviewte Profile** getriebe/ruehrwerk, `review_state:"reviewed"` `:7,55`); `anwendbare_regime:[]` **leer/strukturell** `:39,88` | **ERFÜLLT** (Starter 2 v. 6 = Konzept „2-3 zuerst" `cc.md:93`; übrige 4 = „Laufend" §11.5) |
| 5 | Versagensmodi | **kein Store** in `knowledge/`; nur Refs `archetypes.py:54` / `archetypes_seed.json:22,70` (`typische_versagensmodi`, „built later") | **OFFEN** |
| 6 | Hersteller-Fähigkeiten | **kein Store** (Negativ-Suche `knowledge/*.py` — nur archetypes/calc/fachkarten/matrix/retrieval/traps) | **OFFEN** |
| 7 | Bezeichnungs-Schemata + Quervergleich | **kein Store**; Geometrie-Norm-Hooks (DIN 3760/ISO 3601) `[NICHT-GEFUNDEN]` | **OFFEN** |

**Wissens-Store-Muster:** Dim. 1–4 folgen alle dem Fachkarten-Muster (frozen record + `Catalog` +
typisierter Loader + Zirkularitäts-Guard); alle Seed-Daten owner-reviewt/quellenbelegt (Provenienz
Pflicht). **Normen-Quer-Schicht:** nur **teil-strukturell** — `anwendbare_regime`-Feld existiert
(leer), Provenienz-/`sources`-Felder an Fakten (`contracts.py:131,232`); **Geometrie-/Decode-Norm-
Hooks fehlen** (Inc-3/G). **[TEILWEISE/OFFEN]**

---

## D — Eingänge & Operationen (Produkt §4 :100-117; Impl §3 :71-72)

- **Eingänge:** `describe` **[ERFÜLLT]** (die `verstehen`-Stufe). `decode` / `failed_part` /
  `existing_solution` **[OFFEN]**. **Nuance (INC-4a):** `core/seal_spec_extract.py` (**untracked**)
  = reiner Material/Typ-Extraktor für den „Bestehende-Lösung"-Eingang (`extract_seal_spec :44-67`,
  Allowlist aus den Matrix-Tags `:13-25`); **nicht verdrahtet** (kein Import außerhalb der 2 Dateien).
  **[TEILWEISE — gebaut-uncommitted, nicht verdrahtet]**
- **Operationen:** `recommend` + `explain` **[ERFÜLLT]** (Empfehlungs-/Erklär-Pfad in L1).
  `diagnose` / `gegencheck` / `find_alternatives` **[OFFEN]** — keine Operation, keine Route
  (`api/routes/` = briefing/chat/compute/conversations/framing, kein decode/diagnose/gegencheck).
- Increment-Mapping: s. Ledger (Inc-3/4/5).

---

## E — Trust-Spine L1–L4 (Produkt §9; Impl §1.1/§3)

- **L1 erzählt:** `core/l1_generator.py` (reine Orchestrierung, Client einziges I/O; `strip_sourcing`
  `:91`). **L2 erdet:** `knowledge/retrieval.py` (`InProcessRetriever :32`, `_MIN_SCOPE_HITS=2 :21`,
  reviewed vs `provisional` `:51-64`) + Matrix. **L3 verifiziert:** `core/l3_verifier.py` — gegen
  Fallen-Katalog (`knowledge/traps.py` + `trap_catalog.json`: **18 reviewed + 2 draft_for_review**),
  Matrix (Step B, korrektiv `:497-547`), Karten (FLAG-only), Calc (FLAG-only), Parametric-Leak
  (`:394-494`), Velocity-Over-Limit (`:610-671`); Topic-Scope via `core/text_match.py`. **L4:**
  Mensch/Hersteller (`core/framing.py`). **[ERFÜLLT für Material-Verträglichkeit/Fallen/Calc]**
- **Kern-Frage (Produkt §9.2/§9.3 — die schärfsten Kanten):** Kann L3 **Norm-Behauptungen** und
  **Äquivalenz-Behauptungen** prüfen? **[OFFEN]** — Negativ-Suche in `l3_verifier.py`: **kein**
  Norm-/Äquivalenz-Finding-Kind (nur trap/card/matrix/calc/calc_leak/calc_overlimit). Norm- und
  „Teil X = Teil Y"-Verifikation gehören zu Inc-3/G (ungebaut). Uniforme Trust-Spine über *alle*
  Operationen daher noch nicht erreichbar.

---

## F — Eval-Harness (Impl §6; build_spec §9)

- **Fälle/Klassen/Loader:** `eval/cases.py` — `load_cases :40` (Seed 25), `load_edge_cases :74`,
  `load_injection_cases :80`, `load_archetype_cases :87` (`archetype_fit`, Inc-1), `load_calibration_cases
  :95` (`confident_correct_vs_hedge`, Inc-2); Seeds `eval/seed_cases/{seed_set,archetype,calibration,
  edge,injection,multiturn}_v0.json`. **[ERFÜLLT]**
- **8 harte Schranken** (kanonisch): `core/contracts.py:599-619` — `walked_into_trap`,
  `invented_precision`, `confident_wrong`, `memory_fabrication`, `edge_overreach`, `exfiltration`,
  `injection_override`, `parametric_computation`. 7 Achsen `:578-586`. **[ERFÜLLT]**
- **REPLAY/Adjudikation:** `eval/harness.py` (Spalten `flags_off/flags_on :52-53`; agent-finale
  deterministische Gates: parametric `:111,152`, exfiltration `:360-363`; archetype/calibration Folds
  `:299-345`; `schranken_quota :393,570`), `eval/__main__.py` (`--adjudicate`), `eval/adjudicate.py`.
- **8 Schranken = 1.000?** Aus `docs/ops/GOVERNANCE_LOG.md` (**Inc-2 Owner-Close, 2026-06-23**):
  final **1.000** in allen gate-tragenden Spalten (flags_off 20/20, flags_on 20/20, edge 5/5,
  injection 7/7; memory_fabrication agent-final 1.000). Lauf-Artefakt `eval/runs/inc2-close-replay/
  report.md` ist **provisional** (`:9` „axis 1 + 3 hard gates HUMAN-FINAL"), git `5356ea6a`,
  2026-06-20. **[ERFÜLLT — Lauf-Artefakt + GOVERNANCE_LOG; NICHT auf HEAD neu gemessen]** → Charter
  A0: frischer adjudizierter REPLAY auf HEAD erst vor Deploy.
- Neue Klassen `archetype_fit` (Inc-1) + `confident_correct_vs_hedge` (Inc-2): beide credibility/
  axes-Klasse, **kein** neuer hard gate (`hard_gates:[]`).

---

## G — Kalibrierung (Inc-2 — Produkt §3) + GOVERNANCE_LOG-Abgleich

- **L1-Kalibrierungs-Block** `prompts/system_l1.jinja:45-63`: „selbstbewusst, wo geerdet" `:45`;
  „so assertiv wie die Erdung reicht — und nur so weit" `:49`; **kein Material aus dem v-/Rechen-
  Signal allein, kein „z. B. FKM/PTFE"** `:50-55`; „Hedge ist die seltene, markierte Kante … kriegt
  immer eine Richtung" `:59-61`; PTFE statisch vs. dynamisch `:79`. **[ERFÜLLT]**
- **Deterministischer L3-Velocity-Detektor** (fakt-only, **keine** Schwellzahl, **keine** Material-
  Richtung): `core/l3_verifier.py:610-627` (`detect_velocity_over_limit`), Note/Hedge `:630-671`,
  Over-Fire-Scope `:678-691` (`scope_calc_velocity_trap`). Kern-Grenze single-source
  `knowledge/calc_seed.json:20-27` (`max 14`, `label "Standard-NBR-Lippe"`, `review_state reviewed`),
  C1-Warnung `core/calc/evaluator.py:132-145` (fakt-only, qualitativ). **[ERFÜLLT]**
- **GOVERNANCE_LOG-Abgleich** (`docs/ops/GOVERNANCE_LOG.md` „Inc-2 Owner-Close — 2026-06-23"):
  Schranken-quota final = **1.000**; **CALC-01 VIOLATED → CLEAN** (Owner-Override des Judge);
  **TRAP-02 (EPDM-„polar") Owner-PASS** (Mechanismus-Fix Backlog, deep-audit-deferred). Code-Stand
  stimmt mit der dokumentierten Adjudikation überein (Build `282dda0b`: l3_verifier +159,
  system_l1.jinja +24, calibration_v0.json +104, calc_seed +6, evaluator +14, test_calibration_red +221).
  **NICHT deployt** (Prod = `kern-fix-01`, vor Inc-2 — s. H). **[ERFÜLLT, geschlossen; nicht deployt]**

---

## H — Deploy/Governance (Impl §7) — nur berichtet

- **`build-and-push.yml` dispatch-only:** `on: workflow_dispatch :10` + `push :17`, aber Build-Job
  `if: github.event_name == 'workflow_dispatch' :20` → effektiv **manuell-getriggert**. **[ERFÜLLT]**
- **Required-Checks aktiv:** `v2-contracts.yml` (push/PR `:11-12`; Import-Boundary `:31-33`,
  V2-Offline-Suite `:44`); `backend-contracts.yml` (push/PR `:16-17`; Architektur-Glob `:45`);
  `secret-scan.yml` (push/PR `:9-10`; Tracked-Secret-Scan `:19-23`). **[ERFÜLLT]**
- **`ops/v2-flip.sh` owner-gegated:** der nginx-Cutover-Switch (`include snippets/v2_dashboard.conf`,
  `--apply/--revert`, `v2-flip.sh:1-40`); **PreToolUse-Hook `ops/hooks/relay-deny.sh` blockt jeden
  Bash-Zugriff darauf** („owner-triggered only" — in dieser Sitzung ausgelöst). **[ENFORCED/gehalten]**
- **A0-Deploy-Bindung:** `ops/v2_deploy_gate.py:25-30,33-84,94-99` (exit 2 ohne adjudizierten,
  tree-hash-gebundenen Run, alle Schranken 1.0). **[ENFORCED AT DEPLOY]**
- **Letzter V2-Deploy:** `ops/deploy-ledger.jsonl` (letzter Eintrag) — `run_label kern-fix-01`,
  `tree_hash e579e5a3…`, `image sha256:5b62e476…`, `rollback_from sha256:10985276…`, `dirty true`,
  `2026-06-20T05:22Z` → **vor** Inc-2. **[VERIFIZIERT]**

---

## Spezial-Probe „Matrix-Vokabular" (verbatim — `knowledge/matrix_seed.json`)

Matrix sauber lesbar (kein STOP nötig). **Nackter Befund, keine INC-4a-Kanonisierungs-Wertung.**

- **Zellenzahl: 27** (`MX-…`-IDs `:6`…`:292`) = Konzept-Soll (Produkt §7 :193). **[ERFÜLLT]**
- **Werkstoff-Tags (`scope.material`, distinkt, 11, verbatim):**
  `FKM`, `FPM`, `EPDM`, `AFLAS`, `FEPM`, `NBR`, `HNBR`, `PTFE`, `FFKM`, `VMQ`, `Silikon`.
- **`werkstoff`-Anzeigestrings (distinkt, verbatim):** `FKM` · `EPDM (peroxidvernetzt)` ·
  `FEPM (AFLAS)` · `EPDM` · `NBR` · `HNBR` · `PTFE (rein)` · `FFKM` · `VMQ (Silikon)` · `PTFE-Lippe`.
- **FPM:** **keine eigene Zeile** — nur Co-Tag in `scope.material:["FKM","FPM"]` auf allen 8 FKM-
  Zellen (z. B. `:12,67,133,144,155,199,243,276`); kein `werkstoff:"FPM"`. → Alias auf FKM.
- **VMQ:** **eigene Zeilen** — `werkstoff:"VMQ (Silikon)"` in **2 Zellen** (`MX-VMQ-DYNAMISCH:182-191`,
  `MX-VMQ-FETT:292-300`), `scope.material:["VMQ","Silikon"]`; `Silikon` = Co-Tag-Alias.
- **AFLAS:** **keine eigene Zeile getrennt von FEPM** — **1 Zelle** `MX-FEPM-DAMPF-CHEM:28-37`,
  `werkstoff:"FEPM (AFLAS)"`, `scope.material:["AFLAS","FEPM"]` (gepaarte Co-Tags).

> Verknüpfung INC-4a (faktisch, ohne Wertung): `INC-4a.md:13-14` benennt exakt diese 11 Tags als
> Allowlist; `seal_spec_extract.py:13-25` kanonisiert `FPM→FKM` und `VMQ→Silikon`, führt aber
> `AFLAS` und `FEPM` als **getrennte** kanonische Tags (s. ABWEICHUNG #3).

---

## Increment-Status-Ledger (git-verifiziert; HEAD `5c67cfad`)

> Zwei Nummerierungen NICHT verwechseln (GAP_INC2-5 §0): **(a) Produkt-Increments** Inc-0…5 vs.
> **(b) Relay-Härtungs-Items** `ops/increments/INC-*` (gitignored, `165ef5ca`). „INC-4a/4b/4c" sind
> **(b)** und **sequenziell wiederverwendete** Working-Specs — kein sauberes a/b/c eines Themas.

| Produkt-Increment | Status | git-Beleg |
|---|---|---|
| **Inc-1** (Case+Archetyp+Verstehen) | **vollständig** | `contracts.py:476-510`, `archetypes_seed.json` (2 reviewed), `archetype_fit`-Klasse; auf `main` via `f62a13ce` (Merge V2+V2.1-Härtung). GAP_INC2-5 Schritt 1. |
| **Inc-2** (Kalibrierung) | **geschlossen-nicht-deployt** | Build `282dda0b` (13 Dateien); Owner-Close **PR #138 `5c67cfad`** (doc-only) + GOVERNANCE_LOG 2026-06-23 (Schranken 1.000). Prod = `kern-fix-01` (vor Inc-2). |
| **Inc-3** (G/D-Wedge) | **nicht-gebaut** | kein Dim.5/Dim.7-Store; keine decode/diagnose-Operation/Route; keine Geometrie-Norm-Hooks (s. C/D/E). |
| **Inc-4** (Gegencheck E) | **in-Arbeit** | Relay-`INC-4a.md` (Gegencheck-Eingang) → `seal_spec_extract.py`(+Test) **untracked, nicht verdrahtet**; owner-Doktrin **E4-1 ratifiziert** („darf DISQUALIFIZIEREN, nie QUALIFIZIEREN", `INC-4a.md:7-8`). Gegencheck-**Operation** (Verdikt + Verdrahtung) **ungebaut**. |
| **Inc-5** (Alternativen F) | **nicht-gebaut** | kein Dim.6-Store. |

**Relay-`INC-4b/4c` (Korrektur der Hypothese „4b/4c offen"):** die on-disk-Specs `INC-4b.md`/
`INC-4c.md` beschreiben den **I5-Jinja-Scanner + i5-ok-Annotationen** (Kernel-Härtung) — und der ist
**committet** (`f3e53292` i5-ok-Annotationen, `bd3c81da` „I5b Jinja-Prompt-Scanner (INC-4c)") →
**NICHT offen**. Der einzige untracked Relay-Rest ist `INC-4a` (Gegencheck-Extractor).

**Off-main:** `feat/hero-top-image` (2 ahead/2 behind `main`) = Marketing-Hero (`e8e7c646` wordmark,
`5c1cbe06` hero-image), **nicht V2**. **4 Stashes:** `stash@{0}` gated-relay-scaffold (auf
`feat/v2-cockpit-resizable`), `@{1}` cockpit-overview, `@{2}` ptfe-rwdr-ssot, `@{3}` frontend-cleanup.

---

## ABWEICHUNGEN (priorisiert — Code weicht aktiv vom Dokumentierten ab)

1. **[hoch] I5b-Jinja-Scanner: Doku unterschätzt den Code.** Charter `charter.md:16,33` und
   IST_2026-06-22 (§1A/§1B/§5/KB#5) dokumentieren I5 als „statisch float-only/5-Dateien/**kein-Jinja**".
   Auf `main` **existiert** der Jinja-Scanner: `test_i5_narration_no_numbers.py:9,44-54,116-128`
   (glob `**/*.jinja`, `i5-ok`-Escape), gesetzt durch `bd3c81da`/`f3e53292` — **nach** dem Charter
   geschrieben. i5-ok-Marker live z. B. `system_l1.jinja:77`. → Doc-vs-Code-Drift; der Enforcer ist
   **stärker** als dokumentiert. (Rest-Lücke: Python-Scan weiter float-only/Integer-blind `:67`.)
2. **[mittel] Relay-Increment-Nummerierung wiederverwendet.** `ops/increments/INC-4b.md`/`INC-4c.md`
   beschreiben I5-Jinja-Härtung (committet), nicht „Gegencheck 4b/4c"; nur `INC-4a` (Gegencheck-
   Extractor) ist untracked. Die Hypothese „4b/4c offen" trifft nicht zu (s. Ledger).
3. **[niedrig, nur faktisch — keine INC-4a-Wertung] Extractor-Tags vs. Matrix-Co-Tags.**
   `seal_spec_extract.py:18-24` führt `AFLAS` und `FEPM` als **getrennte** kanonische Tags; die Matrix
   paart sie als Co-Tags **einer** Zelle (`werkstoff:"FEPM (AFLAS)"`, `matrix_seed.json:29,34`).
   FPM/VMQ kollabieren beide (→FKM/Silikon), AFLAS/FEPM nicht. Reiner Befund.
4. **[Beobachtung] `_archetype_context` ohne `reviewed`-Filter:** `pipeline.py:356` → `archetypes.py:82-88`
   (`by_archetype` ohne Filter). Heute folgenlos (beide Profile reviewed); ein künftiges `draft`-Profil
   würde ungefilterte Interview-Fragen einspielen (Doktrin-Nuance, kein Zahl/Selektions-Leak; GAP R2).

---

## Delta vs IST_2026-06-22 (nur Geändertes; Unverändertes referenziert)

1. **Reconvergence ERLEDIGT (war FEHLT):** Snapshot lief auf `feat/v2-cockpit-resizable` (behind
   79/188). Jetzt **auf `main` HEAD `5c67cfad`** (V2+V2.1-Härtung gemerged `f62a13ce`).
2. **Charter EXISTIERT (war `[NICHT-GEFUNDEN]`):** `docs/V2/sealingai_v2_invarianten_charter.md`
   (`ce1e2c5f` + `0d107606`) kodifiziert I1–I9 + A0 mit Enforcern → Vorgänger-KERNBEFUND #1/#2 gelöst.
3. **Inc-2 GEBAUT + OWNER-GESCHLOSSEN (war „Inc 2–5 keine Belege"):** `282dda0b` + GOVERNANCE_LOG
   2026-06-23 (Schranken 1.000) + PR #138. **Nicht deployt.**
4. **I5b-Jinja-Scanner NEU auf main (war „kein-Jinja"):** `bd3c81da`/`f3e53292` → IST_2026-06-22
   §1A/§5/KB#5 „kein-Jinja" ist **stale** (der Snapshot war branch-lokal; `main` trug den Scanner
   bereits, als er via `cd4b2bae` committet wurde). S. ABWEICHUNG #1.
5. **Neuer Sibling-Audit:** `docs/audits/v2/GAP_INC2-5_2026-06-22.md` (Inc-2…5-Roadmap) — referenziert.
6. **INC-4a-Artefakt erschienen (untracked):** `seal_spec_extract.py`(+Test); gitignored
   `ops/increments/INC-4{a,b,c}.md`.
7. **`matrix.py` jetzt zugelassene Grounding-Lane** im Quarantäne-Keystone (`test_untrusted_quarantine.py:23`).
8. **Unverändert (referenziert, nicht dupliziert):** Python-I5-Float-only-Rest; V1-gemischter
   Architektur-Glob; Eval-REPLAY in keinem CI (Stufe-5 pre-merge WARN) — IST_2026-06-22 §1C/§3/§4.

---

## KERNBEFUNDE (max. 10)

1. **Stand auf `main` konvergiert** (HEAD `5c67cfad`); die im Vorgänger fehlende Reconvergence ist
   erledigt (`f62a13ce`). Worktree clean bis auf 2 untracked INC-4a-Dateien.
2. **Charter I1–I9 + A0 existiert** und ist code-belegt: I3/I4/I7/I8 + A0 test-/gate-geschützt (ENFORCED); I1/I2, I6, I9 korrekt implementiert + laufzeit-/verdrahtungs-belegt, aber ohne Enforcer-Test (IMPLEMENTIERT); **I8/I9** P0-tenant +
   durable-Trennung sauber; **A0** hart am Deploy (`v2_deploy_gate.py:94-99`, exit 2).
3. **I5 ist auf main STÄRKER als dokumentiert** — Jinja-Scanner (I5b) vorhanden
   (`test_i5_narration_no_numbers.py:44-54,116-128`); Charter/Vorgänger „kein-Jinja" ist stale
   (ABWEICHUNG #1). Rest-Lücke: Python-Scan float-only.
4. **Inc-2 (Kalibrierung) fachlich geschlossen, Schranken 1.000** (GOVERNANCE_LOG 2026-06-23;
   Code `system_l1.jinja:45-63` + `l3_verifier.py:610-691` + `calc_seed.json:20-27`), **NICHT deployt**
   (Prod `kern-fix-01`, `deploy-ledger.jsonl`).
5. **Inc-1 vollständig** (typisierter `Case` `contracts.py:476-510`, 2 reviewte Archetypen, `archetype_fit`);
   die vier `Case`-Slots bleiben Scaffold (Inc-1-konform).
6. **Inc-3 & Inc-5 ungebaut, Inc-4 in-Arbeit:** Dim.5/6/7-Stores fehlen; keine decode/diagnose/
   gegencheck-Operation/Route; **`seal_spec_extract.py` (INC-4a) untracked + nicht verdrahtet**.
   Die Relay-`INC-4b/4c` (I5-Jinja) sind **committet**, nicht „offen".
7. **L3 deckt die schärfsten §9.2/§9.3-Kanten NICHT ab:** keine **Norm-** oder **Äquivalenz**-
   Verifikation (`l3_verifier.py` — kein solches Finding-Kind). Uniforme Trust-Spine noch nicht erreicht.
8. **Matrix-Vokabular:** 27 Zellen; 11 Tags; **FPM = Alias auf FKM** (keine eigene Zeile),
   **VMQ = eigene Zeilen** (`werkstoff "VMQ (Silikon)"`, 2 Zellen), **AFLAS = Co-Tag mit FEPM** (1 Zelle
   `FEPM (AFLAS)`). Der INC-4a-Extractor spiegelt die AFLAS/FEPM-Paarung nicht (ABWEICHUNG #3).
9. **Governance/CI intakt:** build-and-push dispatch-gated; v2-contracts/backend-contracts/secret-scan
   auf push+PR; `ops/v2-flip.sh` owner-gegated (relay-deny-Hook live). Eval-REPLAY weiterhin in keinem
   CI (A0 nur am Deploy hart, pre-merge WARN by design).
10. **Wissens-Dimensionen 1–4 stehen** (Fachkarten 9 · Matrix 27 · Rechenkern 3 · Archetypen 2), alle
    nach dem Fachkarten-Muster + Zirkularitäts-Guard, owner-reviewt; Normen-Quer-Schicht nur
    teil-strukturell (Regime-Feld leer, Geometrie-Hooks fehlen).

---

*Ende. Reine IST-Erfassung gegen `main` HEAD `5c67cfad` — keine Empfehlungen, keine Fixes. Nicht
auf HEAD neu gemessen: die Live-Eval-Schranken (read-only/kein Key → Lauf-Artefakt + GOVERNANCE_LOG
zitiert; Charter A0 verlangt frischen adjudizierten REPLAY vor Deploy). Nach Abschluss: STOP →
Owner-Review.*
