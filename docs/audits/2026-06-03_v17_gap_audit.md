# V1.7 Gap Audit — `demo/rwdr-limited-external` @ `2bc8c854` — 2026-06-03

> **Messlatte:** V1.7 *Universal Sealing Case Platform* Blueprint (gelesen read-only via `git show origin/feat/v1.7-blueprint:docs/sealing_intelligence_v1_7_universal_sealing_case_platform_blueprint.md`, 592 Z.), V1.6-Contracts (on-branch). Audit **strikt read-only** (Read/Grep/Glob/`git show`); keine Patches. Default-Haltung je Kriterium: **FEHLT bis zum Beweis des Gegenteils**, in beide Richtungen falsifiziert. Belege folgen der Grammatik `[E] path:line — "Zitat"` / `[A]` / `[NV]`.
>
> **Branch-Hinweis:** Das V1.7-*Universal*-Blueprint liegt **nicht** auf diesem Runtime-Branch, sondern nur auf `origin/feat/v1.7-blueprint` (commit `c9c3b47b`) und wurde read-only via `git show` gelesen. Das on-branch `docs/architecture/BLUEPRINT_V1_7_FINAL_CLOSEOUT.md` ist ein **anderes** „V1.7" (Advanced Domain Logic & RAG Coupling) und **nicht** das Audit-Ziel. Die aktive Governance (`CLAUDE.md`/`AGENTS.md`) nennt V1.6/RWDR-MVP als bindend — viele `FEHLT` gegen V1.7-Universal sind daher **„noch nicht gebaut, by design"** (Rule of Three §3.5 / Sequenzierung §9) und erhalten eine Ziel-Phase statt einer Blocker-Eskalation.

## 1. IST-Zustand

Der Runtime ist ein governed V9.2/V10-Stack mit sauberem Single-Writer-State-Gate (Reducer-Kette) und vollständiger RFQ-Readiness-Logik. Tenant-Scoping ist auf den **Case-/Datei-/Evidence-/RFQ-Routen breit durchgesetzt** (query-level `tenant_id`-Filter), aber zwei Nebenflächen brechen aus: die **Memory/LTM-Endpunkte filtern nur nach `user`** (kein Tenant) und die Tenant-Ableitung fällt auf `"default"`/`user_id` zurück. Der Universal-Core ist — wie V1.7 §3.1 selbst feststellt — **größtenteils generisch, aber noch nicht als Core benannt**: RWDR-spezifische **Verzweigungslogik** sitzt in generischen Modulen (`orchestrator.py`, `calculation_projection.py`, `risk_readiness.py`). Ein formales **DomainPack-Interface fehlt** (per Rule of Three §3.5 bis Pack #2 bewusst aufgeschoben), jedoch akkretiert der Core bereits hartcodierte O-Ring/Hydraulik-Zweige — das von §3.3 verbotene „RWDR-Logik in der Plumbing"-Muster in seiner Frühform. Der **RWDR-Killer-Flow ist weitgehend live** (Chat → Cockpit → Pocket-Cockpit → State-Gate, RFQ über dedizierte REST-Endpunkte), aber zwei Felder des Multi-Output-Envelopes (`case_understanding_patch`, `rfq_brief_patch`) bleiben **typisiert-aber-unbefüllt**. Der **Mobile-First-Contract (C8) ist vollständig** (Sofort-Fortschritt, Empty-Spinner-Guard, Degraded-Photo-Führung). **Keine spekulative Abstraktion** (C9 sauber). **Herstellerfeedback (C10) fehlt strukturell vollständig**. Die Wissensschicht trennt Material/Medium/Norm generisch, aber **ohne verpflichtenden Cross-Cutting-vs-Pack-Marker**. Trace-/Tier-Instrumentierung ist nur auf dem Mobile-Pfad vollständig.

## 2. Gap-Matrix

| # | Kriterium (Kurzform) | Verdikt | Kern-Evidenz | Konf. | Schwere | Ziel-Phase |
|---|---|---|---|---|---|---|
| **C1** | Core/Pack-Trennung (kein RWDR in Plumbing) | **ERFÜLLT** (2026-06-04, P1-1 + P1-3) | Seal-Type-Dispatch + alle benannten calc/risk-rwdr-Zweige aus dem Core in den Pack-Seam gezogen (`app/domain/seal_packs.py`; `calculation_projection.py:85`, `risk_readiness.py:437/:464` P1-1; `:527/:555` P1-3 → Pack-Membership). `risk_readiness.py:499` bleibt **bewusst** ein dokumentierter Core-Check (heterogenes Set `{rwdr,ms_pump,unclear_rotary}`, keine 1:1-Pack-Äquivalenz — Owner-Entscheid; ehrlicher Core-Check statt verbogener Abstraktion) | 8 | — | — |
| **C2** | DomainPack-Extension-Point | **ERFÜLLT-Protocol** (2026-06-04, P1-1) | `DomainPack`-Protocol + `RwdrPack` + 1-Eintrag-Seam live (`app/domain/domain_pack.py`, `seal_packs.py`). AC2/§10.1: Protocol JETZT mit RWDR als **einziger** Impl; **Registry-EXTRAKTION** wartet bewusst auf Pack #2 (Rule of Three §3.5) — das ist kein FEHLT mehr | 8 | — | P3+ (Registry) |
| **C3** | Klassifikationsstufe wählt Pack | **ERFÜLLT** (2026-06-04, P1-1) | `normalize_seal_type()`-Output ist jetzt an den Pack-Selektor gebunden (`seal_packs.pack_for(seal_type, seal_family)` → RWDR-Pack); kein hartcodierter Core-Zweig mehr für die RWDR-Auswahl | 7 | — | — |
| **C4** | required_fields domänenspezifisch | **ERFÜLLT** (2026-06-04, P1-1) | **Korrektur:** `orchestrator.py:55` (6 generische SealSystemState-Felder) und `rwdr_mvp_brief.py:115` (31 Brief-Felder) waren **keine Duplikate** — verschiedene Vokabulare/Schichten. Das Core-Tuple ist in die Domain relocated (`app/domain/seal_required_fields.py` → Pack); `_MINIMAL_RWDR_FIELDS` bleibt separater Owner | 9 | — | — |
| **C5** | Cross-Cutting vs Domain-Wissen getrennt | **TEILWEISE** | generische Repos + `[E] rag/rag_schema.py:52 — "domain: Domain = Domain.MATERIAL"`, aber **kein** verpflichtender Cross-Cutting-vs-Pack-Marker | 6 | MEDIUM | P2 |
| **C6** | Tenant-Scoping auf allen Case/Datei/Evidence/RFQ-Ops | **ERFÜLLT** (2026-06-04, P0-1+P0-2) | LTM tenant-scoped (P0-1, `memory_core.py`) **und** alle Request-Tenant-Fallbacks entfernt → strikt **401** via `require_tenant_id` (P0-2: `deps.py:23`, `rfq.py:42`, `memory.py:40`, `rag.py:57`, `chat_history` ×4); Shared-Tenant-Pfade (`RAG_SHARED_TENANT_ID`) by-design erhalten; 374 Legacy-Cases → `sealai` migriert; live `no-claim → 401` belegt | 8 | — | — |
| **C7** | RWDR-Killer-Flow durchgängig live | **ERFÜLLT-per-Amendment** (2026-06-04, P0-3) | Kette live (Chat→Cockpit→Pocket→State-Gate; RFQ via REST). Owner-Entscheid Option 2: Liefervertrag = `pocket_cockpit_patch`(+single-source `rfq_status`) + `rfq_readiness_projection` + `cockpit_patch` + REST-One-Pager; die toten Stubs `case_understanding_patch`/`rfq_brief_patch` **entfernt** (contracts.py + Blueprint §11/§28.2 amendiert) | 9 | — | — |
| **C8** | Mobile <1s Fortschritt + Degraded-Photo-Führung | **ERFÜLLT** | `[E] mobile_triage.py:166 — "\"first_progress_ms\": 0"`; `[E] PocketCockpit.tsx:56 — "if (isLoading && !hasContent(patch))"`; `build_visual_low_confidence_guidance()`; (echte <1s = `[NV]`) | 9 | — | — |
| **C9** | Keine spekulative Abstraktion über RWDR | **ERFÜLLT** | keine `o_ring/`/`flat_gasket/`-Scaffolds; Single-RWDR-Impl ist intendiert (§3.5) | 9 | — | — |
| **C10** | Herstellerfeedback strukturell aufgenommen | **FEHLT** | 0 Treffer für `manufacturer_response`/`partner_response`/Feedback-Intake in `app`; nur `[E] state.py:197 — "@router.post(\"/state/workspace/partner-select\" …)"` (liefert 501) | 8 | MEDIUM | P2 |
| **S1** | Trace-Felder (§6.1) | **TEILWEISE** | `[E] contracts.py:380-424 — "class TraceSummary"` (alle Pflichtfelder typisiert), aber `first_progress_ms`/`latency_ms` nur auf Mobile-Pfad befüllt | 8 | MEDIUM | P1 |
| **S2** | Tier-Disziplin (§6.2) | **TEILWEISE** | Vertrags-Disziplin (`action_chip_binding` reine Reducer, `rag_used=False`), aber **kein** nicht-umgehbarer Runtime-Enforcer gegen Tier-0→RAG | 6 | MEDIUM | P1 |
| **S3** | State-Gate Single-Writer | **ERFÜLLT** | `[E] reducers.py — "the ONLY functions that may produce NormalizedState, AssertedState, or GovernanceState"`; kein Bypass gefunden | 9 | — | — |
| **S4** | RFQ-Readiness (§20) | **ERFÜLLT** | `[E] rfq_one_pager.py:20-33` (5 Bänder) + `evaluate_rfq_readiness()` + `[E] :325 — "def build_rfq_snapshot(*, case_revision: int …)"` | 10 | — | — |
| **S5** | Mode-Coverage (§5) | **TEILWEISE** | `[E] contracts.py:15-27 — "TurnRoute = Literal[…]"` (11 Routen) vs ~30 §5-Modes; Konsolidierung in Dispatch/Templates, Namens-Divergenz | 6 | LOW | P1 |

## 3. Befunde im Detail

### C1 — Core/Pack-Trennung · TEILWEISE · HIGH · P1
RWDR-**Verzweigungslogik** (nicht nur Konstanten/Labels) lebt in generisch benannten Modulen:
- `[E] backend/app/agent/v92/orchestrator.py:325 — "def _required_fields_for(seal_type: str, seal_family: str) -> tuple[str, ...]:"` mit `[E] :331-336` Dispatch → `_RWDR_REQUIRED_FIELDS`/`_ORING_REQUIRED_FIELDS`/`_HYDRAULIC_REQUIRED_FIELDS`.
- `[E] backend/app/agent/v92/calculation_projection.py:85 — "if calc_id.startswith(\"rwdr.\") or calc_id == \"rwdr\":"`.
- `[E] backend/app/agent/domain/risk_readiness.py:437 — "elif pressure > 1 and str(engineering_path or \"\") == \"rwdr\":"` plus `[E] :464 — "if calc_id not in {\"rwdr_pv_precheck\", \"rwdr_dn_value\", \"rwdr_circumferential_speed\"}"`.

Mildernd: die **Tiefe** ist korrekt gekapselt — `[E] backend/app/agent/domain/rwdr_calc.py:1 — "RWDR Deterministic Calculation Core."` und `backend/app/services/rwdr_mvp_brief.py` sind saubere Pack-Module. **Durchsuchte Alternativen:** `services/calculation_engine.py` (PTFE_RWDR_* — Pack-spezifisch, akzeptabel), Tests/Fixtures/UI-Labels (ausgeschlossen). **Grenzfall-Hinweis:** Da Core **Branching** (nicht nur Konstanten) enthält, liegt C1 an der TEILWEISE/FEHLT-Grenze; als TEILWEISE gewertet, weil die RWDR-Tiefe isoliert ist. **Fix-Richtung:** Seal-Type-Dispatch hinter einen Domain-Selektor ziehen statt im Core hartzucodieren.

### C2 — DomainPack-Extension-Point · FEHLT · MEDIUM · P1→P3+
Kein `DomainPack`-Protocol/Registry. `[E] backend/app/agent/capability_registry/contracts.py:119 — "class CapabilityModule(Protocol)"` und `[E] backend/app/agent/domain/governed_data.py:94 — "class DomainDataProvider(Protocol)"` sind **generische Ports**, kein Pack-Extension-Point (kein `pack_id`/`classification_signals`/`required_fields`). **Gedankenexperiment (o_ring):** zu **modifizierende** Dateien: `orchestrator.py` (Dispatch), `calculation_projection.py` (`_is_rwdr_ledger_item`), `checks_registry.py`, `risk_readiness.py`, `rwdr_mvp_brief.py`-Scope. **Wichtige Einordnung:** V1.7 §3.5 (Rule of Three) **schiebt die Registry bewusst bis Pack #2 (P3+) auf** — das FEHLT ist daher größtenteils by-design. **Eigentliche Sorge:** der Core akkretiert bereits hartcodierte O-Ring/Hydraulik-Zweige (`orchestrator.py:63/72`) — das §3.3-Anti-Pattern in Frühform. **Fix-Richtung:** Core-Grenze *benennen* und neue hartcodierte Typ-Zweige stoppen; Registry erst bei O-Ring extrahieren.

### C3 — Klassifikationsstufe wählt Pack · TEILWEISE · MEDIUM · P1
Ein deterministischer `normalize_seal_type()` existiert und wird konsumiert (`_required_fields_for`, `build_seal_system_state`), aber **kein `SealTypeImpactAgent`** und — mangels Packs — **kein Pack zur Auswahl**. Die Klassifikation steuert hartcodierte Zweige statt eines Packs. **Fix-Richtung:** Normalizer-Output an einen Pack-Selektor binden, sobald Packs existieren.

### C4 — required_fields domänenspezifisch · TEILWEISE · MEDIUM · P1
Domain-owned Mindestkern vorhanden und von der RFQ-Readiness konsumiert: `[E] backend/app/services/rwdr_mvp_brief.py:115 — "_MINIMAL_RWDR_FIELDS: tuple[str, ...] = ("` + `[E] :149 — "_CRITICAL_REQUIREMENTS: dict[str, tuple[str, ...]] = {"`. **Aber dupliziert** mit core-hartcodiertem `[E] backend/app/agent/v92/orchestrator.py:55 — "_RWDR_REQUIRED_FIELDS = ("` → zwei Quellen der Wahrheit. **Fix-Richtung:** Core-Tuple entfernen, Orchestrator den Domain-Mindestkern abfragen lassen.

**✅ P1-1 (2026-06-04) — C1–C4 adressiert, verhaltensneutral, in 3 Mini-PRs.**
- **PR1** — Seal-Required-Field-Seam aus dem Core relocated: `app/domain/seal_required_fields.py` (Daten-Tupel) + `app/domain/seal_packs.py` (Selektor). Core hardcodet keine Feldlisten mehr.
- **PR2** — `DomainPack`-Protocol (`app/domain/domain_pack.py`) + `RwdrPack` + 1-Eintrag-Seam (`seal_packs.py`); O-Ring/Hydraulik als explizit markierte **SHALLOW STUBS** + Stopp-Schild. Nur Methoden mit echter RWDR-Impl; `failure_modes`/`risk_flags` bewusst weggelassen (kein Pack-Level-Impl — surfaced).
- **PR3** — calc/risk-rwdr-Erkennung über Pack-Membership: `calculation_projection.py:85`, `risk_readiness.py:437/:464` → `pack_for_calc_id`/`pack_for_engineering_path`/`is_pack_calculation`.
- **Korrektur C4:** `orchestrator.py:55` (6 Felder) ≠ `rwdr_mvp_brief.py:115` (31 Felder) — **kein Duplikat**, verschiedene Schichten; die „dupliziert"-Lesart war ungenau.
- **C2-Lesart (AC2/§10.1):** Das Protocol soll JETZT existieren — mit RWDR als einziger Impl. Nur die **Registry-Maschinerie** (N Packs) wartet auf Pack #2 (Rule of Three §3.5). Beleg-/Verhaltensneutralität: Charakterisierungstests `test_p1_1_required_fields_characterization.py`, `test_p1_1_domain_pack_seam.py`, `test_p1_1_pr3_calc_risk_pack_membership.py` (grün vor+nach); volle Backend-Suite EXIT=0 je PR; doctrine-reviewer APPROVE über PR2+PR3.
- ~~**Restbefund (surfaced, NICHT actioned):** `risk_readiness.py:498/:527/:555`~~ → **✅ ABGESCHLOSSEN (2026-06-04, P1-3):** `:527`/`:555` (clean `== "rwdr"`) auf `pack_for_engineering_path` umgestellt; `:499` (heterogenes Set `{rwdr,ms_pump,unclear_rotary}`) **bewusst als dokumentierter Core-Check belassen** (Owner-Entscheid — keine saubere Pack-Zugehörigkeit). Charakterisierungs-Freeze grün vor+nach; doctrine-reviewer APPROVE; deployed `…@sha256:afb82cfb…`. Siehe `docs/architecture/CORE_PACK_BOUNDARY.md`.

### C5 — Cross-Cutting vs Domain-Wissen · TEILWEISE · MEDIUM · P2
Material/Chemie-Repos sind generisch; `[E] backend/app/services/rag/rag_schema.py:52 — "domain: Domain = Domain.MATERIAL"` kategorisiert (MATERIAL/STANDARD/PRODUCT/TROUBLESHOOTING), aber **kein verpflichtender `is_cross_cutting`/Pack-Affinitäts-Marker** trennt querschnittliches von typspezifischem Wissen. **Fix-Richtung:** Pflicht-Flag in der ChunkMetadata/Knowledge-Payload ergänzen und Pack-spezifisches Wissen routen.

### C6 — Tenant-Scoping · TEILWEISE (mit CRITICAL-Befund) · P0
**Positive Abdeckung bestätigt:** Case-/RFQ-/RAG-Routen filtern query-level — `.where(CaseRecord.tenant_id == tenant_id).where(CaseRecord.user_id == user_id)`, Redis-Namespacing `governed_state:{tenant_id}:{session_id}`, `.where(RagDocument.tenant_id == …)`; Tenant-Ableitung zentral in `backend/app/services/auth/dependencies.py:76-81 _resolve_tenant_id`. Von ~39 Case/Datei/Evidence/RFQ-Routen sind ~35 query-verifiziert.

**CRITICAL — Memory/LTM ohne Tenant-Filter:** `[E] backend/app/services/memory/memory_core.py:63-65 — "def _build_user_filter(...)" → "FieldCondition(key=\"user\", match=MatchValue(value=user))"`, genutzt von `[E] :87 ltm_export_all`/`[E] :132 ltm_delete_all` und `[E] backend/app/api/v1/endpoints/memory.py:59 — "\"user\": user.user_id"`. Kein `tenant_id` im Qdrant-Payload/Filter. **Repro-Skizze (nicht ausgeführt):** spannt ein `user_id` zwei Tenants (Multi-Realm oder `AUTH_USER_ID_CLAIM=email`), liest/löscht `GET/DELETE /memory` tenant-übergreifend. **Refutationsversuch:** nach `tenant_id`-Übergabe an `ltm_*` gesucht → nicht gefunden; Exploit-**Vorbedingung** (Wiederkehr von `user_id` über Tenants) ist konfig-abhängig (`[A]`, Konf. ~7), das **fehlende Filter** selbst ist Konf. 9.

**HIGH — `"default"`-Fallback:** `[E] backend/app/agent/api/deps.py:23 — "tenant_id = current_user.tenant_id or \"default\""` → ohne Tenant-Claim teilen sich alle Nutzer den `default`-Namespace (Case/Workspace-Routen). **MEDIUM — `user_id`-Fallback:** `[E] backend/app/api/v1/endpoints/rfq.py:42 — "tenant_id = str(user.tenant_id or user.user_id or \"\").strip()"` (sicher bei `sub`, riskant bei `email`). **Paperless-Webhook: refutiert** — `backend/app/services/rag/paperless.py:141 RAG_SHARED_TENANT_ID` ist by-design Shared-Tenant, **kein** Cross-Tenant-Leak (von der CRITICAL-Liste gestrichen). **Fix-Richtung:** `tenant_id` in LTM-Payload+Filter; fehlenden Tenant-Claim mit 401 ablehnen statt `"default"`.

**✅ RESOLVED (2026-06-04) — P0-1 + P0-2.** P0-1 hat LTM tenant-gescoped (Qdrant-Payload+Filter; Prod-Deploy 2026-06-04T06:00Z). P0-2 hat die Request-Tenant-Fallbacks zentralisiert und entfernt: neuer Single-SoT `backend/app/services/auth/dependencies.py require_tenant_id` wirft bei fehlendem/leerem Claim **401** (`missing_tenant_claim`); umgestellt: `deps.py:23`, `rfq.py:42`, `memory.py:40`, `rag.py:57`, `chat_history.py` ×4. **Shared-Tenant by-design erhalten** (`RAG_SHARED_TENANT_ID`/Paperless — Invariant-Test). Red-before-green: `backend/tests/unit/services/test_p0_2_strict_tenant_resolver.py` (8 rot → grün). Bestands-Migration `ops/migrations/p0_2_unify_tenant_to_sealai.sql` (idempotent, dry-run-default): 374 Legacy-Cases (353 `default` + 21 echte Realm-User) + zugehörige `mutation_events`/`outbox` → `sealai`; pg_dump-Backup zuvor. Live deployed `…@sha256:6916d557…`; in-container `no-claim → 401` belegt. **Onboarding-Konsequenz:** `registrationAllowed=true` → neue/attributlose User bekommen 401, bis Admin `tenant_id` setzt (Runbook `docs/ops/KEYCLOAK_TENANT_ID_MAPPER.md`).

### C7 — Killer-Flow durchgängig live · TEILWEISE · MEDIUM · P0/P1
Kette belegt: Input `backend/app/agent/api/routes/chat.py` (`chat`/`chat/stream`-Handler) → Router `backend/app/agent/v92/turn_boundary.py:193` → State-Gate `backend/app/agent/state/reducers.py` → Envelope `backend/app/agent/v92/contracts.py:299 AssistantTurnEnvelope` + `backend/app/agent/v92/dashboard_contract.py:226 build_v92_dashboard_contract` → SSE `backend/app/agent/api/streaming.py` → **Desktop-Cockpit `[E] frontend/src/components/dashboard/SealCockpit.tsx` existiert + wired** (korrigiert eine frühere Inventar-Fehlannahme) → Pocket `frontend/src/components/dashboard/PocketCockpit.tsx` → RFQ `backend/app/agent/communication/rfq_one_pager.py` via REST `rfq.py` (`export.md/.pdf`, `/rwdr/cases/{id}/brief`). **Stubbed:** `[E] backend/app/agent/v92/contracts.py:303 — "case_understanding_patch: dict[str, Any] | None = None"` und `[E] :304 — "rfq_brief_patch … = None"` haben **keinen Writer** (Grep-bestätigt); der RFQ-One-Pager wird gebaut, aber **nicht in den gestreamten Envelope** gepackt. Das Cockpit rendert dennoch (aus `cockpit_patch`). **Fix-Richtung:** beide Sub-Patches im Response-Builder befüllen — oder `cockpit_patch`+REST explizit als Liefervertrag dokumentieren.

**✅ ERFÜLLT-per-Amendment (2026-06-04, P0-3 — Owner Option 2).** Liefervertrag dokumentiert und implementiert: in-Turn `pocket_cockpit_patch` (recognized/critical/next_step/**rfq_status**) + `rfq_readiness_projection` (readiness) + `cockpit_patch` (Desktop) + REST-One-Pager on demand. Die Stubs `case_understanding_patch`/`rfq_brief_patch` sind **entfernt** (`contracts.py`, Blueprint §11/§28.2). **Einziger echter Bug**, dabei gefunden+gefixt: das gestreamte `rfq_status` war hartcodiert `"DRAFT"` und das UI leitete bei fehlendem Feld lokal ab → jetzt single-source aus `evaluate_rfq_readiness` (Backend `pocket_rfq_status_from_state` an der Streaming-Stelle; Frontend rendert den Backend-Wert, lokale Ableitung nur als Fallback). Red-before-green: `test_p0_3_pocket_rfq_status_single_source.py` (Backend) + `pocketCockpit.test.ts` (Frontend). **Korrektur eines früheren Audit-Fehlbefunds:** `PocketCockpit.tsx` rendert `rfq_status` bereits (Zeile 187-193) — der frühere C7-Fact-Check-Befund "rendert nicht" beruhte auf einem abgeschnittenen Grep.

### C10 — Herstellerfeedback · FEHLT · MEDIUM · P2
Keine Intake-Endpunkte, kein Persistenz-Modell, kein Fluss ins Wissen (0 Treffer `manufacturer_response`/`partner_response`/`*_feedback` in `app`). Einziges Artefakt: `[E] backend/app/api/v1/endpoints/state.py:197 — "@router.post(\"/state/workspace/partner-select\" …)"` (liefert 501, deaktiviert). **Einordnung:** V1.7 §4.5/§9 markiert die Feedback-Schleife explizit als **P2** — FEHLT ist erwartungsgemäß deferred, kein Blocker. **Fix-Richtung:** strukturiertes Feedback-Modell + Endpunkt + designierter Pfad in querschnittliches/domänenspezifisches Wissen.

### S1/S2/S5 (S1+S2 ERFÜLLT 2026-06-04 P1-2; S5 offen)
- **S1 — ✅ ERFÜLLT (2026-06-04, P1-2 TEIL A).** War: `first_progress_ms`/`latency_ms` nur Mobile. Jetzt **eine zentrale Messstelle** im Streaming-Layer: `app/agent/runtime/turn_timing.py` (Contextvar-Timer), gestartet in `streaming.py event_generator`; `SSEEventBuilder.event()` markiert Time-to-first-chunk und stempelt `first_progress_ms`/`latency_ms` auf das finale `state_update` für **alle** TurnRoutes aus derselben Quelle. Mobile-Trace byte-identisch erhalten (nested `first_progress_ms=0`). Beleg: `test_p1_2_turn_timing_trace.py` (Schwellen-Sanity); volle Suite EXIT=0.
- **S2 — ✅ ERFÜLLT (2026-06-04, P1-2 TEIL B).** War: nur vertragliche Tier-Disziplin, kein Enforcer. Jetzt **fail-closed Guard** am Retrieval-Funnel: `app/agent/runtime/turn_tier.py` (`TierViolation`, Contextvar-Tier, Kill-Switch `SEALAI_TIER0_RETRIEVAL_GUARD` default ON, Incident-only/geloggt) + **EIN** `enforce_retrieval_allowed()` am Eingang von `rag_orchestrator.hybrid_retrieve` (deckt alle 3 Runtime-Caller). Tier-0 = {GREETING, META_QUESTION, BLOCKED} (Owner, strict-safe), deklariert in `dispatch.py` aus der Pre-Gate-Klassifikation. Red-before-green: `test_p1_2_tier0_retrieval_guard.py` (Tier-0-Retrieval heute erfolgreich → danach `TierViolation`). False-Trip-Beweis: Szenario-Matrix S1-S10 + Golden + volle Suite + Live-Smoke → **0 TierViolation auf legitimen Pfaden**. doctrine-reviewer APPROVE. Live deployed `…@sha256:808e5cae…`; in-container Tier-0→TierViolation belegt. Doku Kill-Switch: `.claude/rules/ops.md`.
- **S5:** `[E] backend/app/agent/v92/contracts.py:15-27 TurnRoute = Literal[…]` (11 Routen) vs. ~30 §5-Modes; Sheet-/Visual-/Micro-Modes sind Template-/Dispatch-Ebene, keine First-Class-Routen; Namens-Divergenz (`case_building`↔`engineering_case_update`). Strukturelle Divergenz, kein bestätigter Funktionsverlust. P1.

## 4. Top-5-Risiken (mit Blast Radius)
1. **Memory/LTM Cross-Tenant (CRITICAL, P0)** — Blast: jeder LTM-`export`/`delete`/`create`; bei Multi-Tenant-User-Wiederkehr tenant-übergreifendes Lesen/Löschen von Erinnerungen. `memory_core.py:65`.
2. ~~**`tenant_id → "default"`-Fallback (HIGH, P0)**~~ → **✅ GESCHLOSSEN (2026-06-04, P0-2):** strikt 401 via `require_tenant_id`; 374 Legacy-Cases → `sealai` migriert; deployed. War: alle governed-State-/Case-Routen über `_canonical_scope`. `deps.py:23`.
3. **RWDR-Branching im Core (HIGH, P1)** — Blast: `orchestrator.py`/`calculation_projection.py`/`risk_readiness.py`; jeder weitere Dichtungstyp akkretiert Hartcode-Zweige (§3.3-Anti-Pattern), steigende Pack-Kosten.
4. ~~**Unvollständiger Streamed-Envelope (MEDIUM, P0/P1)**~~ → **✅ GESCHLOSSEN (2026-06-04, P0-3 Option 2):** Liefervertrag = pocket + readiness + cockpit + REST dokumentiert; tote Stubs entfernt; `rfq_status` single-sourced. War: `contracts.py:303-304`.
5. ~~**Lücken in Trace/Tier-Instrumentierung (MEDIUM, P1)**~~ → **✅ GESCHLOSSEN (2026-06-04, P1-2):** zentrale Trace-Messstelle (S1) + fail-closed Tier-0-Retrieval-Guard (S2), beide deployed `…@sha256:808e5cae…`. War: `first_progress_ms`/`latency_ms` nur Mobile, kein Tier-0-RAG-Guard.

## 5. Empfohlene Patch-Reihenfolge (klein, einzeln testbar — nur Titel + Ziel)
- **P0-1 — LTM tenant-scopen.** Ziel: `tenant_id` in Qdrant-Payload + `_build_user_filter`; Cross-Tenant-Memory-Leck schließen.
- **P0-2 — Tenant-Fallbacks entfernen.** ✅ **APPLIED 2026-06-04.** Ziel: fehlenden Tenant-Claim mit 401 ablehnen statt `"default"`/`user_id`; kein kollidierbarer Namespace. (strikt via `require_tenant_id`; 374 Cases → `sealai` migriert; deployed `@sha256:6916d557…`.)
- **P0-3 — Streamed-Envelope vervollständigen.** Ziel: `case_understanding_patch` + `rfq_brief_patch` befüllen (oder Liefervertrag dokumentieren); §11-Contract in-Turn erfüllen.
- **P1-1 — Core-Grenze benennen, Core-Branching stoppen.** Ziel: required-fields/risk/projection über einen Domain-Selektor; neue Typ-Hartcode-Zweige unterbinden (C1–C4).
- **P1-2 — Trace/Tier fertigstellen.** ✅ **APPLIED 2026-06-04.** Ziel: `first_progress_ms`/`latency_ms` über alle Pfade + nicht-umgehbarer Tier-0-RAG-Guard (S1/S2). (zentrale Streaming-Messstelle + fail-closed Guard an `hybrid_retrieve`; deployed `@sha256:808e5cae…`.)
- **P2-1 — Knowledge-Marker + Herstellerfeedback.** Ziel: Cross-Cutting-vs-Pack-Flag (C5) + strukturierte Feedback-Aufnahme & -Fluss (C10).

## 6. Offene Fragen, Annahmen [A], Nicht-Prüfbares [NV]
- `[NV]` **Echte <1s-Mobile-Latenz (C8)** — read-only nur der Mechanismus prüfbar; gemessene Latenz nicht.
- ~~`[NV]` **Prod-JWT-Claims / `.env`**~~ → **AUFGELÖST (2026-06-04):** Keycloak-Mapper `tenant_id` live am `nextauth`-Client (alle 6 Realm-User tragen `tenant_id=sealai`), per `generate-example-access-token` belegt. C6-Fallbacks sind damit entfernbar — und entfernt (P0-2). Neue/attributlose User → 401 (Onboarding-Konsequenz, s. Runbook).
- `[A]` **C6-Exploitierbarkeit Memory/LTM** hängt an `AUTH_USER_ID_CLAIM` (`sub` vs `email`) und Multi-Tenant-User-Mitgliedschaft; der **fehlende Filter** ist Konf. 9, der **Cross-Tenant-Impact** konfig-bedingt Konf. ~7.
- ~~`[A]` **C2/C3 sind größtenteils by-design**~~ → **ADRESSIERT (2026-06-04, P1-1):** Die Benennung/Trennung ist gebaut (DomainPack-Protocol + Pack-Seam, RWDR-only). Nur der Bau der **Registry** bleibt by-design bis Pack #2 (Rule of Three §3.5). Siehe `docs/architecture/CORE_PACK_BOUNDARY.md`.
- ~~**Offene Frage:** In-Turn-Befüllung von `case_understanding_patch`/`rfq_brief_patch`?~~ → **ENTSCHIEDEN (2026-06-04, Owner Option 2):** `pocket_cockpit_patch` + `rfq_readiness_projection` + `cockpit_patch` + REST-One-Pager sind der akzeptierte Liefervertrag; die Stubs werden NICHT befüllt, sondern entfernt.

## 7. Coverage-Erklärung (was wurde NICHT auditiert)
- **Nicht ausgeführt:** App/Tests/Build (read-only-Vertrag) — Funktions-/Render-Korrektheit über die reine Verdrahtung hinaus, echte Latenzen → `[NV]`.
- **Tenant-Tiefe:** die 4 verdächtigen Lücken wurden voll re-walked; die ~35 „scoped" Routen wurden über das geteilte `_request_tenant_id` + Persistenz-Filter **stichprobenartig** bestätigt, nicht jede einzeln re-walked.
- **Nicht im Fokus (separate Reviews):** Prompt-Injection-Härtung-Tiefe, Upload-Path-Traversal/Content-Type, MCP-Tool-Call-Tenant-Scoping (delegiert), Rate-Limiting/DoS, Style/Deps — per Hard-Exclusions ausgeschlossen.
- **Doku als Intent, nicht als Presence** gewertet; RWDR-Strings in Tests/Fixtures/Docs/UI-Labels nie als C1-Verletzung.

---

*Erstellt strikt read-only (Read/Grep/Glob/`git show`). Keine Patches in dieser Session. Methodik: Phase-0-Preconditions → 3× Inventar (read-only Explore) → 4× Evidenz-Härtung/Falsifikation → Synthese; Evidenz-Grammatik `[E]/[A]/[NV]` durchgesetzt.*
