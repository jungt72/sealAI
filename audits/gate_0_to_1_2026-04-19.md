# Gate 0→1 Audit Report

**Gate:** Sprint 0 → Sprint 1 transition
**Date:** 2026-04-19
**Auditor:** Claude Code (read-only, via founder-executed commands)
**Scope:** Verify that Sprint 0 patches (0.1 API-key rotation, 0.2 COI firewall log, 0.3 baseline capture) are complete and that the repository state is ready for Sprint 1 persistence foundation work.
**Verdict:** **PASS WITH CONDITIONS** — Sprint 1 may start AFTER the three pre-Sprint-1 remediation items in §7 are handled.

---

## 1. Executive summary

Sprint 0 achieved its primary objectives: the COI firewall log is committed (commit `b195286b`), the Phase 1a baseline is committed (commit `a80d0055`, 845 lines), and API-key rotation is reported as complete by the founder. Historical git exposure of API keys was investigated and found clean — all historical matches are template placeholders or dummy values, not real secrets.

However, the baseline revealed three blocking gaps that must be addressed before Sprint 1 patches can meaningfully execute with the authority-required discipline ("tests green before every merge"). These are documented in §7 as a small Sprint 0.5 task set.

**Sprint 1 is not unconditionally cleared.** It is cleared subject to Sprint 0.5 completion.

---

## 2. Checks executed and verdicts

### 2.1 Key hygiene (Check-Block A)

| Check | Command basis | Result | Verdict |
|-------|---------------|--------|---------|
| A.1 — Key patterns in git history | `git log -p --all` grep for real key prefixes | 5 matches, all template/dummy (`CHANGE_ME`, `sk-...`, `sk-dummy`, `sk-proj-CHANGE_ME`, `dein_schlüssel`) | PASS |
| A.2 — Generic key-length pattern | Regex `sk-[a-zA-Z0-9]{20,}|lsv2_[a-zA-Z0-9]{20,}` | 1 match, identified as SHA256 hash from a LangChain package RECORD file, not a key | PASS (false positive) |
| A.3 — `.env` gitignored | `grep -E env .gitignore` | `.env*` glob at line 2 correctly matches `.env` | PASS |
| A.4 — `.env` never tracked | `git ls-files .env` | Empty output | PASS |
| A.5 — No untracked files with key content | `git status --short` | Clean | PASS |
| A.6 — Content of the one generic match | Grep with context | SHA256 in package manifest, not a credential | PASS |
| A.7 — Commits containing real key assignments | Filtered grep on patches | Zero commits with real-looking key bodies | PASS |
| A.8 — `.env.example` templates tracked | `git ls-files .env` variants | Five `.env.example`-style templates properly tracked | PASS |

**Finding:** Zero verified cases of real API key exposure in git history. Five template placeholders confirmed as template-only. `.gitignore` protection for `.env` is effective (via `.env*` glob). Key hygiene is sound.

### 2.2 COI firewall log (Check-Block B, items B.1–B.3)

| Check | Result | Verdict |
|-------|--------|---------|
| B.1 — All 8 sections present | 8/8 sections (Purpose, Scope, Declaration, Review Triggers, Legal, Enforcement, Annex A, Signature) | PASS |
| B.2 — Four assurance sub-points in §3 | 4/4 present verbatim (3.1 No proprietary data, 3.2 PTFE-KB-JSONs public, 3.3 Golden cases generic, 3.4 Qdrant non-employer) | PASS |
| B.3 — Critical legal/strategic terms | 7/7 terms present (§17 UWG, BGB, Moat Layer 1, Thorsten Jung, 2026-04-18, Annex A, Supplement v2) | PASS |

**Finding:** The COI firewall log at `konzept/coi_firewall_log.md` (92 lines, commit `b195286b`) matches Founder Decision #3's implementation implications in full. Legally and strategically complete for its purpose as a self-declared firewall record.

### 2.3 Baseline document structure (Check-Block B items B.4–B.5)

| Check | Result | Verdict |
|-------|--------|---------|
| B.4 — All 15 sections present | 15/15 sections (Git State through Baseline Status Summary) | PASS |
| B.5 — Cross-reference section populated | Section 13 exists, references the Phase 1a Audit Report, documents 10 authority findings with concrete metrics | PASS WITH ANMERKUNG |

**Anmerkung B.5:** Codex chose a paragraph/code-block format for Section 13 rather than the formal table (`| Audit Finding | Measurement | Match? |`) specified in Patch 0.3. The content is equivalent in information value but diverges from the prompt template. For future baseline captures, the table format should be enforced. No remediation required for this baseline.

### 2.4 Baseline-vs-Audit consistency (Check-Block C)

Spot-checks verified that the baseline's measurements match the 2026-04-17 Phase 1a Audit Report findings.

| Audit finding | Baseline measurement | Match? |
|---------------|----------------------|--------|
| `output_contract_node.py` oversized at ~1335 lines | 1335 LOC confirmed | YES |
| `matching_node.py` ~471 lines | 471 LOC confirmed | YES |
| `intake_observe_node.py` ~454 lines | 454 LOC confirmed | YES |
| `rfq_handover_node.py` ~379 lines | 379 LOC confirmed | YES |
| `services/langgraph/` with YAML rules exists | Confirmed; actual YAML paths are `domains/rwdr/schema.yaml` and `domains/hydraulics_rod/schema.yaml` — different path than assumed in prompt, but structurally present | YES (with note) |
| `services/fast_brain/` exists | Confirmed | YES |
| `_legacy_v2/` exists | Confirmed at `backend/app/_legacy_v2` | YES |
| `ResultForm` enum present in `agent/runtime/policy.py` | Confirmed, line 14, with 50 total references | YES |
| `RoutingPath` present | Confirmed, 74 total references | YES |
| No `mutation_events` / `outbox` / `risk_scores` tables | Confirmed absent | YES |
| No `ManufacturerProfile` / `ManufacturerCapabilityClaim` / `GenericConcept` / `ProductTerm` references | 0 references in code — classes do not exist | YES |
| No `sealing_material_family` references | 0 references — column does not exist yet | YES |
| `engineering_path` established | 29 references — correctly in use as baseline enum | YES (expected) |

**Finding:** Ten of ten authority findings confirmed consistent between baseline and audit. No contradictions. One format deviation noted (YAML rule path structure) that will shape Sprint 5 Patch 5.1 methodology but does not affect Sprint 1.

---

## 3. Sprint 0 deliverable summary

| Deliverable | Target | Status | Commit |
|-------------|--------|--------|--------|
| Patch 0.1 — API keys rotated | All old keys revoked, new keys active, services healthy | COMPLETE (founder self-report, no independent verification possible) | N/A (not a git commit) |
| Patch 0.2 — COI firewall log | `konzept/coi_firewall_log.md` committed | COMPLETE | `b195286b` |
| Patch 0.3 — Phase 1a baseline | `audits/phase_1a_baseline_2026-04-18.md` committed | COMPLETE | `a80d0055` |
| Schema dump companion | `audits/sealai_schema_2026-04-18.sql` | NOT CAPTURED — command failed (invalid compose project) | blocker for Sprint 1 Patch 1.1 |

---

## 4. Documented findings (non-blocking)

Items found during audit that are noteworthy but do not block Sprint 1 transition:

**F.1 — Historical key templates in git.** Five commits over history contain lines like `OPENAI_API_KEY=sk-...` or `OPENAI_API_KEY=CHANGE_ME` in example files. These are legitimate template placeholders. No action required. Informational only.

**F.2 — `.gitignore` has redundancy.** Lines 2, 85, 86 of `.gitignore` overlap in env-file coverage. Cosmetic issue. Low-priority cleanup candidate for a future chore commit.

**F.3 — YAML rule paths differ from prompt assumption.** Sprint 5 Patch 5.1 methodology assumed `services/langgraph/rules/common.yaml` and `rules/rwdr.yaml`. Reality: `services/langgraph/domains/rwdr/schema.yaml` and `domains/hydraulics_rod/schema.yaml`. This changes the Sprint 5 migration scope (schema files describe structure, not executable rules in the traditional sense). Implementation Plan Sprint 5 Patch 5.1 will need a brief re-scoping when that sprint is reached. **Not a Sprint 1 concern.**

**F.4 — Cross-reference format deviation.** Baseline Section 13 uses prose + code blocks instead of a formal markdown table. Acceptable for this baseline. For Gate 1→2 and later, prompts should be explicit about format enforcement if desired.

---

## 5. Blocking findings (must address before Sprint 1)

The following items are blockers for meaningful Sprint 1 work and must be resolved first.

### 5.1 Schema dump not captured (BLOCKER for Sprint 1 Patch 1.1)

Sprint 1 Patch 1.1 introduces an Alembic migration that extends the `cases` table. Without a current-state schema dump, we have no authoritative reference for the current column inventory of that table. The Codex-attempted command `docker compose exec -T postgres pg_dump --schema-only sealai` failed because the working directory lacks a docker-compose project file pointing at the postgres service.

**Remediation:** Direct `docker exec` against the running `postgres` container, capture the dump, commit.

**Owner:** Founder (30-minute manual task; Codex is not needed).

### 5.2 Test suite uncollectable (BLOCKER for "tests green" discipline)

13 test files fail at pytest's collection stage before any test logic runs. This means the test runner cannot even enumerate what tests exist. Without a working test runner, Sprint 1's core discipline ("no patch merges with failing tests") cannot be enforced.

Files affected:
- `backend/tests/agent/test_rag_injection.py`
- `backend/tests/agent/test_selection.py`
- `backend/tests/agent/test_version_provenance.py`
- `backend/tests/test_audit_logger.py`
- `backend/tests/test_golden_set.py`
- `backend/tests/test_langgraph_compile.py`
- `backend/tests/test_mcp_calc_engine.py`
- `backend/tests/test_p4_live_calc.py`
- `backend/tests/test_paperless_sync.py`
- `backend/tests/test_param_snapshot.py`
- `backend/tests/test_parameter_guardrails.py`
- `backend/tests/test_parameter_lww.py`
- `backend/tests/unit/test_number_verification.py`

**Remediation options (founder choice):**

- **Option A** — Diagnose and fix the collection errors (likely import-time issues from refactored modules). Sprint 0.5 Patch for Codex, 1-3 hours.
- **Option B** — Mark these tests as "to be replaced in Sprint 1" and create `backend/tests/new/` as fresh tree. Sprint 1 services get new tests; legacy tests are quarantined until Sprint 5 cleanup. 30 minutes.
- **Option C** — Hybrid: quick fix for collection (imports), defer logic fixes to Sprint 5. 1 hour.

**Recommended:** Option C. Quick fixes unblock the runner. Deeper test-logic issues are addressed naturally as services are refactored.

### 5.3 Mypy not installed (BLOCKER for type-safety discipline)

AGENTS.md §28.3 and CLAUDE.md §9 reference type-safety as part of validation. Mypy is not installed in the shell. For Sprint 1's new domain/model/service code, type-checking should be in place from the start.

**Remediation:** Add mypy to `pyproject.toml` or `requirements-dev.txt`, configure minimal `mypy.ini`, run once to establish baseline. 15 minutes.

---

## 6. Open items (monitor but not blocking)

**M.1 — Ruff 364 findings.** Current codebase has significant linter backlog. Sprint 1 new code should aim for zero-ruff-findings on new files. Legacy findings addressed opportunistically as code is refactored.

**M.2 — Docker setup non-standard.** The server hosts multiple unrelated stacks (crm-stack, erpnext-stack, prelon_postgres). The SeaLAI-relevant containers (backend, postgres, qdrant, keycloak, etc.) appear to run directly via `docker run` rather than a dedicated compose file. This is fine operationally but means commands that reference `docker compose` for SeaLAI context will consistently fail. Codex prompts from Sprint 1 onward should use direct `docker exec` against named containers.

**M.3 — Legacy shell scripts in repo root.** 13 tracked shell scripts (backup, cleanup, purge, strapi-related) are historical. Candidates for Sprint 5 cleanup review but not touched during Phase 1a otherwise.

---

## 7. Sprint 0.5 — Pre-Sprint-1 remediation plan

Before Sprint 1 starts, execute these three remediation tasks. Total estimated effort: 2-4 hours.

### 7.1 Sprint 0.5 Patch A — Schema dump

**Owner:** Founder (manual).

**Command** (to be verified against actual DB credentials):

```bash
cd ~/sealai

# Identify actual DB name and user from .env
grep -E "POSTGRES|DATABASE|DB_" .env

# Dump schema (adjust DB name and user based on .env):
docker exec postgres pg_dump --schema-only -U <user> <dbname> > audits/sealai_schema_2026-04-19.sql

# Verify
wc -l audits/sealai_schema_2026-04-19.sql
head -20 audits/sealai_schema_2026-04-19.sql
grep -E "^CREATE TABLE" audits/sealai_schema_2026-04-19.sql

# Commit
git add audits/sealai_schema_2026-04-19.sql
git commit -m "audit: capture current postgres schema as Sprint 1 reference"
```

**Acceptance:** file exists, >100 lines, shows CREATE TABLE cases and case_state_snapshots definitions.

### 7.2 Sprint 0.5 Patch B — Test collection fix (Codex)

**Owner:** Codex CLI, Option C scope.

**Prompt outline** (full prompt to be written before execution):

> Read 13 listed test files. For each, run `python -c "import <test_module>"` to identify the import-time failure. Categorize: (a) fixable-by-import-adjustment, (b) fixable-by-dependency-install, (c) references-removed-module-must-skip. Fix (a) and (b) conservatively. For (c), add `pytestmark = pytest.mark.skip(reason="Legacy test, module removed, to be replaced in Sprint X")`. Goal: `pytest backend/tests/ --collect-only -q` succeeds with zero collection errors. Do NOT fix failing assertions. Do NOT rewrite test logic. Only fix collectability.

**Acceptance:** `pytest backend/tests/ --collect-only -q` returns 0 errors. Commit. Document in a short note file under `audits/`.

### 7.3 Sprint 0.5 Patch C — Mypy baseline

**Owner:** Codex CLI.

**Prompt outline:**

> Add `mypy` to `requirements-dev.txt` (or equivalent). Create minimal `mypy.ini` with `python_version = 3.11`, `ignore_missing_imports = True`, `check_untyped_defs = False` (for now, to keep baseline manageable). Run `mypy backend/app/` once. Commit the config + a baseline-findings file under `audits/`. Do NOT fix any type errors. Do NOT touch other dependencies.

**Acceptance:** `mypy --version` returns version string. `mypy.ini` exists and is minimal. Commit on baseline branch.

---

## 8. Gate verdict

**Verdict: PASS WITH CONDITIONS**

- Sprint 0 primary objectives: ACHIEVED
- Security baseline: VERIFIED CLEAN
- Documentation artifacts (COI, baseline): SOUND
- Consistency with Phase 1a Audit Report: CONFIRMED

- Blocker 5.1 (schema dump): must complete before Sprint 1 Patch 1.1
- Blocker 5.2 (test collection): must complete before first Sprint 1 test-requiring patch (likely Patch 1.4 or 1.6)
- Blocker 5.3 (mypy): should complete before Sprint 1 first service module

**Authorization:** Sprint 0.5 may begin immediately. Sprint 1 may begin after Sprint 0.5 completion.

---

## 9. Founder decisions requested

Before Sprint 0.5 executes, founder should confirm:

1. **Schema dump option** — Confirm that `docker exec postgres pg_dump ...` is acceptable (versus using a `docker compose exec` with a new project file). Recommendation: use direct `docker exec` per M.2.

2. **Test-fix strategy** — Confirm Option C (quick collection fix, defer logic). Alternative options A or B exist.

3. **Mypy configuration strictness** — Confirm starting with permissive config (`check_untyped_defs = False`, `ignore_missing_imports = True`) and tightening later, versus stricter-from-start.

---

## 10. Next actions

1. Founder reviews this Gate 0→1 report
2. Founder responds to §9 decisions
3. Sprint 0.5 patches execute in order (A → B → C)
4. Post-0.5 verification: Claude Code spot-check that remediation is in place
5. Sprint 1 commences with Patch 1.1 (Alembic migration for extended cases table)

---

**Report end.**
