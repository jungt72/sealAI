services:
  # Production-only overrides for the shared development services. Keeping
  # these controls out of docker-compose.yml preserves the local dev stack;
  # the production overlay is fail-closed on exact digests and measured limits.
  postgres:
    image: ${POSTGRES_IMAGE:?POSTGRES_IMAGE required - use an immutable name@sha256 digest}
    read_only: true
    tmpfs:
    - /run/postgresql:rw,noexec,nosuid,nodev,uid=999,gid=999,mode=0770
    - /tmp:rw,noexec,nosuid,nodev,uid=999,gid=999,mode=1770
    security_opt:
    - no-new-privileges:true
    cap_drop:
    - ALL
    cap_add:
    - CHOWN
    - DAC_OVERRIDE
    - FOWNER
    - SETGID
    - SETUID
    pids_limit: ${POSTGRES_PIDS_LIMIT:?POSTGRES_PIDS_LIMIT required after bounded-load measurement}
    mem_limit: ${POSTGRES_MEMORY_LIMIT:?POSTGRES_MEMORY_LIMIT required after bounded-load measurement}
    cpus: ${POSTGRES_CPU_LIMIT:?POSTGRES_CPU_LIMIT required after bounded-load measurement}
    logging:
      driver: local
      options:
        max-size: ${CONTAINER_LOG_MAX_SIZE:-10m}
        max-file: ${CONTAINER_LOG_MAX_FILES:-5}
    networks:
    - postgres_keycloak_network
    - postgres_backend_network
    - strapi_postgres_network
  redis:
    image: ${REDIS_IMAGE:?REDIS_IMAGE required - use an immutable name@sha256 digest}
    read_only: true
    tmpfs:
    - /run:rw,noexec,nosuid,nodev,mode=0755
    - /tmp:rw,noexec,nosuid,nodev,mode=1770
    security_opt:
    - no-new-privileges:true
    cap_drop:
    - ALL
    cap_add:
    - CHOWN
    - SETGID
    - SETUID
    pids_limit: ${REDIS_PIDS_LIMIT:?REDIS_PIDS_LIMIT required after bounded-load measurement}
    mem_limit: ${REDIS_MEMORY_LIMIT:?REDIS_MEMORY_LIMIT required after bounded-load measurement}
    cpus: ${REDIS_CPU_LIMIT:?REDIS_CPU_LIMIT required after bounded-load measurement}
    logging:
      driver: local
      options:
        max-size: ${CONTAINER_LOG_MAX_SIZE:-10m}
        max-file: ${CONTAINER_LOG_MAX_FILES:-5}
    networks:
    - redis_network
  qdrant:
    image: ${QDRANT_IMAGE:?QDRANT_IMAGE required - use an immutable name@sha256 digest}
    environment:
      QDRANT__SERVICE__API_KEY: ${QDRANT_API_KEY:?QDRANT_API_KEY required - provision only through GATE-02/GATE-05}
    volumes:
    - qdrant_snapshots:/qdrant/snapshots
    read_only: true
    tmpfs:
    - /tmp:rw,noexec,nosuid,nodev,mode=1770
    security_opt:
    - no-new-privileges:true
    cap_drop:
    - ALL
    pids_limit: ${QDRANT_PIDS_LIMIT:?QDRANT_PIDS_LIMIT required after bounded-load measurement}
    mem_limit: ${QDRANT_MEMORY_LIMIT:?QDRANT_MEMORY_LIMIT required after bounded-load measurement}
    cpus: ${QDRANT_CPU_LIMIT:?QDRANT_CPU_LIMIT required after bounded-load measurement}
    logging:
      driver: local
      options:
        max-size: ${CONTAINER_LOG_MAX_SIZE:-10m}
        max-file: ${CONTAINER_LOG_MAX_FILES:-5}
    networks:
    - qdrant_backend_network
  gotenberg:
    image: ${GOTENBERG_IMAGE:?GOTENBERG_IMAGE required - use an immutable name@sha256 digest}
    read_only: true
    tmpfs:
    - /tmp:rw,noexec,nosuid,nodev,mode=1770
    - /home/gotenberg:rw,noexec,nosuid,nodev,mode=0700
    security_opt:
    - no-new-privileges:true
    cap_drop:
    - ALL
    pids_limit: ${GOTENBERG_PIDS_LIMIT:?GOTENBERG_PIDS_LIMIT required after bounded-load measurement}
    mem_limit: ${GOTENBERG_MEMORY_LIMIT:?GOTENBERG_MEMORY_LIMIT required after bounded-load measurement}
    cpus: ${GOTENBERG_CPU_LIMIT:?GOTENBERG_CPU_LIMIT required after bounded-load measurement}
    logging:
      driver: local
      options:
        max-size: ${CONTAINER_LOG_MAX_SIZE:-10m}
        max-file: ${CONTAINER_LOG_MAX_FILES:-5}
    networks:
    - document_network
  tika:
    image: ${TIKA_IMAGE:?TIKA_IMAGE required - use an immutable name@sha256 digest}
    read_only: true
    tmpfs:
    - /tmp:rw,noexec,nosuid,nodev,mode=1770
    security_opt:
    - no-new-privileges:true
    cap_drop:
    - ALL
    pids_limit: ${TIKA_PIDS_LIMIT:?TIKA_PIDS_LIMIT required after bounded-load measurement}
    mem_limit: ${TIKA_MEMORY_LIMIT:?TIKA_MEMORY_LIMIT required after bounded-load measurement}
    cpus: ${TIKA_CPU_LIMIT:?TIKA_CPU_LIMIT required after bounded-load measurement}
    logging:
      driver: local
      options:
        max-size: ${CONTAINER_LOG_MAX_SIZE:-10m}
        max-file: ${CONTAINER_LOG_MAX_FILES:-5}
    networks:
    - document_network
  prometheus:
    read_only: true
    tmpfs:
    - /tmp:rw,noexec,nosuid,nodev,mode=1770
    security_opt:
    - no-new-privileges:true
    cap_drop:
    - ALL
    pids_limit: ${PROMETHEUS_PIDS_LIMIT:?PROMETHEUS_PIDS_LIMIT required after bounded-load measurement}
    mem_limit: ${PROMETHEUS_MEMORY_LIMIT:?PROMETHEUS_MEMORY_LIMIT required after bounded-load measurement}
    cpus: ${PROMETHEUS_CPU_LIMIT:?PROMETHEUS_CPU_LIMIT required after bounded-load measurement}
    logging:
      driver: local
      options:
        max-size: ${CONTAINER_LOG_MAX_SIZE:-10m}
        max-file: ${CONTAINER_LOG_MAX_FILES:-5}
    networks:
    - observability_network
    - backend_metrics_network
  grafana:
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:?GRAFANA_ADMIN_PASSWORD required - rotate through GATE-02}
    read_only: true
    tmpfs:
    - /tmp:rw,noexec,nosuid,nodev,mode=1770
    security_opt:
    - no-new-privileges:true
    cap_drop:
    - ALL
    pids_limit: ${GRAFANA_PIDS_LIMIT:?GRAFANA_PIDS_LIMIT required after bounded-load measurement}
    mem_limit: ${GRAFANA_MEMORY_LIMIT:?GRAFANA_MEMORY_LIMIT required after bounded-load measurement}
    cpus: ${GRAFANA_CPU_LIMIT:?GRAFANA_CPU_LIMIT required after bounded-load measurement}
    logging:
      driver: local
      options:
        max-size: ${CONTAINER_LOG_MAX_SIZE:-10m}
        max-file: ${CONTAINER_LOG_MAX_FILES:-5}
    networks:
    - observability_network
  frontend:
    profiles:
    - frontend-container
    # depends_on: backend removed 2026-06-25 (V1 backend retired → profile v1-legacy). The
    # BACKEND_INTERNAL_URL/SEALAI_BACKEND_ORIGIN below point at the retired host but the V1 backend
    # saw ZERO product traffic over 24h, so they are inert; left in place for an easy re-enable.
    restart: unless-stopped
    expose:
    - '3000'
    environment:
      BACKEND_INTERNAL_URL: http://backend:8000
      SEALAI_BACKEND_ORIGIN: http://backend:8000
      NEXTAUTH_DEBUG: ${NEXTAUTH_DEBUG:-false}
      NEXTAUTH_URL: ${NEXTAUTH_URL:?NEXTAUTH_URL required — set in .env or .env.prod}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET:?NEXTAUTH_SECRET required — set in .env or
        .env.prod}
      AUTH_SECRET: ${AUTH_SECRET:?AUTH_SECRET required — set in .env or .env.prod}
      KEYCLOAK_ISSUER: ${KEYCLOAK_ISSUER:?KEYCLOAK_ISSUER required — set in .env or
        .env.prod}
      KEYCLOAK_CLIENT_ID: ${KEYCLOAK_CLIENT_ID:?KEYCLOAK_CLIENT_ID required — set
        in .env or .env.prod}
      KEYCLOAK_CLIENT_SECRET: ${KEYCLOAK_CLIENT_SECRET:?KEYCLOAK_CLIENT_SECRET required
        — set in .env or .env.prod}
    healthcheck:
      test:
      - CMD-SHELL
      - wget -qO- http://127.0.0.1:3000/api/health >/dev/null
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    image: ${FRONTEND_IMAGE:?FRONTEND_IMAGE required — set pinned image ref in .env.prod}
    pull_policy: ${FRONTEND_PULL_POLICY:-always}
    networks:
      frontend_network:
        aliases:
        - sealai-frontend
      frontend_egress_network:
        gw_priority: 1
    user: 1001:1001
    read_only: true
    tmpfs:
    - /tmp:rw,noexec,nosuid,nodev,uid=1001,gid=1001,mode=1770
    - /app/.next/cache:rw,noexec,nosuid,nodev,uid=1001,gid=1001,mode=0700
    security_opt:
    - no-new-privileges:true
    cap_drop:
    - ALL
    pids_limit: ${FRONTEND_PIDS_LIMIT:?FRONTEND_PIDS_LIMIT required after bounded-load measurement}
    mem_limit: ${FRONTEND_MEMORY_LIMIT:?FRONTEND_MEMORY_LIMIT required after bounded-load measurement}
    cpus: ${FRONTEND_CPU_LIMIT:?FRONTEND_CPU_LIMIT required after bounded-load measurement}
    logging:
      driver: local
      options:
        max-size: ${CONTAINER_LOG_MAX_SIZE:-10m}
        max-file: ${CONTAINER_LOG_MAX_FILES:-5}
  keycloak:
    container_name: keycloak
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/${KC_DB_NAME:?KC_DB_NAME required - use the dedicated Keycloak database}
      KC_DB_USERNAME: ${KC_DB_USERNAME:?KC_DB_USERNAME required - use the dedicated Keycloak database role}
      # KCRAW preserves literal '$' sequences instead of treating them as
      # SmallRye Config expressions. Keycloak and V2 use independent roles;
      # the credentials are provisioned only through the gated DB cutover.
      KCRAW_DB_PASSWORD: ${KC_DB_PASSWORD:?KC_DB_PASSWORD required - rotate/provision only through GATE-02/GATE-07}
      KC_PROXY_HEADERS: xforwarded
      KC_HTTP_ENABLED: 'true'
      KC_HOSTNAME: ${KC_HOSTNAME:-https://sealingai.com}
      KC_HOSTNAME_STRICT: 'true'
      KC_HEALTH_ENABLED: 'true'
      KC_METRICS_ENABLED: 'true'
    expose:
    - '8080'
    - '9000'
    image: ${KEYCLOAK_IMAGE:?KEYCLOAK_IMAGE required — set pinned image ref in .env.prod}
    pull_policy: ${KEYCLOAK_PULL_POLICY:-always}
    healthcheck:
      test:
      - CMD-SHELL
      - >-
        /bin/bash -ec 'exec 3<>/dev/tcp/127.0.0.1/9000;
        printf "HEAD /health/ready HTTP/1.0\r\n\r\n" >&3;
        IFS= read -r status <&3;
        [[ "$$status" == *" 200 "* ]]'
      interval: 15s
      timeout: 5s
      retries: 12
      start_period: 60s
    networks:
      keycloak_edge_network:
        aliases:
        - sealai-keycloak
      backend_identity_network:
        aliases:
        - sealai-keycloak
      postgres_keycloak_network: {}
      keycloak_egress_network:
        gw_priority: 1
    read_only: true
    tmpfs:
    - /tmp:rw,noexec,nosuid,nodev,uid=1000,gid=0,mode=1770
    - /opt/keycloak/data:rw,noexec,nosuid,nodev,uid=1000,gid=0,mode=0700
    security_opt:
    - no-new-privileges:true
    cap_drop:
    - ALL
    pids_limit: ${KEYCLOAK_PIDS_LIMIT:?KEYCLOAK_PIDS_LIMIT required after bounded-load measurement}
    mem_limit: ${KEYCLOAK_MEMORY_LIMIT:?KEYCLOAK_MEMORY_LIMIT required after bounded-load measurement}
    cpus: ${KEYCLOAK_CPU_LIMIT:?KEYCLOAK_CPU_LIMIT required after bounded-load measurement}
    logging:
      driver: local
      options:
        max-size: ${CONTAINER_LOG_MAX_SIZE:-10m}
        max-file: ${CONTAINER_LOG_MAX_FILES:-5}
  nginx:
    image: ${NGINX_IMAGE:?NGINX_IMAGE required - use an immutable name@sha256 digest}
    container_name: nginx
    depends_on:
      keycloak:
        condition: service_healthy
    restart: unless-stopped
    ports:
    - 80:80
    - 443:443
    volumes:
    - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    - ./nginx/00-tuning.conf:/etc/nginx/conf.d/00-tuning.conf:ro
    - ./nginx/www:/var/www/certbot:ro
    - /etc/letsencrypt:/etc/letsencrypt:ro
    - ./nginx/certs/config:/etc/letsencrypt-analytics:ro
    # V2 cutover mounts — INERT until `include snippets/v2_dashboard.conf;` exists in
    # default.conf. Nginx mounts the release ROOT (not a mutable dist tree and not the
    # source-side symlink itself), so an approved atomic `current` replacement is visible
    # inside the existing container and rollback remains digest-addressed.
    - ./nginx/snippets:/etc/nginx/snippets:ro
    - /var/lib/sealai/dashboard-releases:/usr/share/nginx/dashboard-releases:ro
    healthcheck:
      test:
      - CMD-SHELL
      - test -s /var/run/nginx.pid || exit 1
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
    - edge_network
    - frontend_network
    - backend_network
    - keycloak_edge_network
    read_only: true
    tmpfs:
    - /var/cache/nginx:rw,noexec,nosuid,nodev,uid=101,gid=101,mode=0750
    - /var/run:rw,noexec,nosuid,nodev,uid=101,gid=101,mode=0750
    - /tmp:rw,noexec,nosuid,nodev,mode=1770
    security_opt:
    - no-new-privileges:true
    cap_drop:
    - ALL
    cap_add:
    - CHOWN
    - NET_BIND_SERVICE
    - SETGID
    - SETUID
    pids_limit: ${NGINX_PIDS_LIMIT:?NGINX_PIDS_LIMIT required after bounded-load measurement}
    mem_limit: ${NGINX_MEMORY_LIMIT:?NGINX_MEMORY_LIMIT required after bounded-load measurement}
    cpus: ${NGINX_CPU_LIMIT:?NGINX_CPU_LIMIT required after bounded-load measurement}
    logging:
      driver: local
      options:
        max-size: ${CONTAINER_LOG_MAX_SIZE:-10m}
        max-file: ${CONTAINER_LOG_MAX_FILES:-5}
  # V2 green-field backend (cutover arc) — OFF by default behind the `v2` profile; defined HERE
  # (not only in an overlay) so `up -d --remove-orphans` with the standard file set never treats a
  # running backend-v2 as an orphan. Routed only after `ops/v2-flip.sh --apply` enables the nginx
  # snippet. Secrets via --env-file interpolation only (no wholesale .env.prod injection).
  backend-v2:
    container_name: backend-v2
    profiles:
    - v2
    image: ${BACKEND_V2_IMAGE:?BACKEND_V2_IMAGE required - use an immutable name@sha256 digest}
    pull_policy: ${BACKEND_V2_PULL_POLICY:-missing}
    build:
      context: ./backend
      dockerfile: Dockerfile.v2
    user: 10001:10001
    restart: unless-stopped
    environment:
      APP_ENV: ${APP_ENV:-production}
      PYTHONUNBUFFERED: '1'
      # V2-owned auth (M6c validate-in-V2): JWKS fetched on the internal network (no hairpin
      # through nginx); the issuer is the public realm URL (KC_HOSTNAME pins the `iss` claim).
      SEALAI_V2_AUTH_JWKS_URL: ${SEALAI_V2_AUTH_JWKS_URL:-http://sealai-keycloak:8080/realms/sealAI/protocol/openid-connect/certs}
      SEALAI_V2_AUTH_ISSUER: ${SEALAI_V2_AUTH_ISSUER:-https://sealingai.com/realms/sealAI}
      SEALAI_V2_AUTH_AUDIENCE: ${SEALAI_V2_AUTH_AUDIENCE:-sealai-v2}
      SEALAI_V2_AUTH_JWKS_TTL_S: ${SEALAI_V2_AUTH_JWKS_TTL_S:-300}
      SEALAI_V2_AUTH_JWKS_MAX_TTL_S: ${SEALAI_V2_AUTH_JWKS_MAX_TTL_S:-600}
      SEALAI_V2_AUTH_JWKS_UNKNOWN_KID_REFRESH_INTERVAL_S: ${SEALAI_V2_AUTH_JWKS_UNKNOWN_KID_REFRESH_INTERVAL_S:-5}
      SEALAI_V2_AUTH_JWKS_NEGATIVE_KID_TTL_S: ${SEALAI_V2_AUTH_JWKS_NEGATIVE_KID_TTL_S:-30}
      SEALAI_V2_AUTH_JWKS_MAX_NEGATIVE_KIDS: ${SEALAI_V2_AUTH_JWKS_MAX_NEGATIVE_KIDS:-256}
      SEALAI_V2_AUTH_MAX_TOKEN_AGE_S: ${SEALAI_V2_AUTH_MAX_TOKEN_AGE_S:-300}
      SEALAI_V2_AUTH_CLOCK_SKEW_S: ${SEALAI_V2_AUTH_CLOCK_SKEW_S:-30}
      # AUTH-001 is default-DENY. Enabling requires migration 0011 plus the digest of a reviewed
      # worst-case provider price/call/retry contract; Settings refuses incomplete activation.
      SEALAI_V2_PROVIDER_REQUESTS_ENABLED: ${SEALAI_V2_PROVIDER_REQUESTS_ENABLED:-false}
      SEALAI_V2_PROVIDER_SUBJECT_REQUESTS_PER_MINUTE: ${SEALAI_V2_PROVIDER_SUBJECT_REQUESTS_PER_MINUTE:-6}
      SEALAI_V2_PROVIDER_TENANT_REQUESTS_PER_MINUTE: ${SEALAI_V2_PROVIDER_TENANT_REQUESTS_PER_MINUTE:-30}
      SEALAI_V2_PROVIDER_SUBJECT_REQUESTS_PER_DAY: ${SEALAI_V2_PROVIDER_SUBJECT_REQUESTS_PER_DAY:-100}
      SEALAI_V2_PROVIDER_TENANT_REQUESTS_PER_DAY: ${SEALAI_V2_PROVIDER_TENANT_REQUESTS_PER_DAY:-1000}
      SEALAI_V2_PROVIDER_TENANT_REQUESTS_PER_MONTH: ${SEALAI_V2_PROVIDER_TENANT_REQUESTS_PER_MONTH:-20000}
      SEALAI_V2_PROVIDER_SUBJECT_MAX_CONCURRENT: ${SEALAI_V2_PROVIDER_SUBJECT_MAX_CONCURRENT:-2}
      SEALAI_V2_PROVIDER_TENANT_MAX_CONCURRENT: ${SEALAI_V2_PROVIDER_TENANT_MAX_CONCURRENT:-10}
      SEALAI_V2_PROVIDER_REQUEST_LEASE_S: ${SEALAI_V2_PROVIDER_REQUEST_LEASE_S:-300}
      SEALAI_V2_PROVIDER_REQUEST_RESERVATION_MICROS: ${SEALAI_V2_PROVIDER_REQUEST_RESERVATION_MICROS:-250000}
      SEALAI_V2_PROVIDER_DAILY_BUDGET_MICROS: ${SEALAI_V2_PROVIDER_DAILY_BUDGET_MICROS:-10000000}
      SEALAI_V2_PROVIDER_MONTHLY_BUDGET_MICROS: ${SEALAI_V2_PROVIDER_MONTHLY_BUDGET_MICROS:-100000000}
      SEALAI_V2_PROVIDER_BUDGET_CONTRACT_SHA256: ${SEALAI_V2_PROVIDER_BUDGET_CONTRACT_SHA256:-}
      SEALAI_V2_API_MAX_REQUEST_BODY_BYTES: ${SEALAI_V2_API_MAX_REQUEST_BODY_BYTES:-131072}
      # Ops hardening (2026-07-03 deep-dive re-audit, §3): these two gate real runtime access
      # (api/deps.py — the Hersteller-Partner admin/manufacturer surface) but had no compose
      # passthrough, so a `.env.prod` override would have silently done nothing. Defaults match
      # settings.py's own defaults, so this is a pure no-op today — additive-only, zero behavior
      # change until someone actually needs to override a Keycloak role name.
      SEALAI_V2_AUTH_ADMIN_ROLE: ${SEALAI_V2_AUTH_ADMIN_ROLE:-admin}
      SEALAI_V2_AUTH_MANUFACTURER_ROLE: ${SEALAI_V2_AUTH_MANUFACTURER_ROLE:-manufacturer}
      SEALAI_V2_AUTH_CAPABILITY_REVIEWER_ROLE: ${SEALAI_V2_AUTH_CAPABILITY_REVIEWER_ROLE:-capability_reviewer}
      SEALAI_V2_AUTH_KNOWLEDGE_REVIEWER_ROLE: ${SEALAI_V2_AUTH_KNOWLEDGE_REVIEWER_ROLE:-knowledge_reviewer}
      SEALAI_V2_AUTH_DECISION_REVIEWER_ROLE: ${SEALAI_V2_AUTH_DECISION_REVIEWER_ROLE:-decision_reviewer}
      # Ops hardening (2026-07-03): the 6 documented "incident-only kill-switches" (their own
      # settings.py docstrings say they're meant to be env-flippable in an emergency) had no
      # compose passthrough either — same gap, higher stakes (a real incident is exactly the
      # moment you don't want to discover a silent no-op). Defaults match settings.py's own
      # defaults (all True) — pure no-op today, only matters if ever actually flipped.
      SEALAI_V2_UNDERSTAND_ENABLED: ${SEALAI_V2_UNDERSTAND_ENABLED:-true}
      SEALAI_V2_VERIFY_ENABLED: ${SEALAI_V2_VERIFY_ENABLED:-true}
      SEALAI_V2_GROUND_ENABLED: ${SEALAI_V2_GROUND_ENABLED:-true}
      SEALAI_V2_COMPATIBILITY_MATRIX_ENABLED: ${SEALAI_V2_COMPATIBILITY_MATRIX_ENABLED:-false}
      SEALAI_V2_COMPUTE_ENABLED: ${SEALAI_V2_COMPUTE_ENABLED:-true}
      SEALAI_V2_MEMORY_ENABLED: ${SEALAI_V2_MEMORY_ENABLED:-true}
      SEALAI_V2_DISTILL_ENABLED: ${SEALAI_V2_DISTILL_ENABLED:-true}
      OPENAI_API_KEY: ${OPENAI_API_KEY:?OPENAI_API_KEY required — set in .env or .env.prod}
      # V2 deterministic-first execution: Mistral Small 4 is the standard answer tier;
      # GPT-5.5 is reserved for complex/high-risk routed cases. The runtime-profile hash binds
      # this entire policy and model set to the adjudicated replay used by the release gate.
      SEALAI_V2_PROVIDER: ${SEALAI_V2_PROVIDER:-mistral}
      SEALAI_V2_L1_PROVIDER: ${SEALAI_V2_L1_PROVIDER:-openai}
      SEALAI_V2_L1_MODEL: ${SEALAI_V2_L1_MODEL:-gpt-5.5}
      SEALAI_V2_STANDARD_PROVIDER: ${SEALAI_V2_STANDARD_PROVIDER:-mistral}
      SEALAI_V2_STANDARD_MODEL: ${SEALAI_V2_STANDARD_MODEL:-mistral-small-2603}
      SEALAI_V2_VERIFIER_PROVIDER: ${SEALAI_V2_VERIFIER_PROVIDER:-mistral}
      SEALAI_V2_VERIFIER_MODEL: ${SEALAI_V2_VERIFIER_MODEL:-mistral-small-2603}
      SEALAI_V2_HELPER_PROVIDER: ${SEALAI_V2_HELPER_PROVIDER:-mistral}
      SEALAI_V2_HELPER_MODEL: ${SEALAI_V2_HELPER_MODEL:-mistral-small-2603}
      SEALAI_V2_JUDGE_PROVIDER: ${SEALAI_V2_JUDGE_PROVIDER:-openai}
      SEALAI_V2_JUDGE_MODEL: ${SEALAI_V2_JUDGE_MODEL:-gpt-5.4-mini-2026-03-17}
      SEALAI_V2_EVAL_JUDGE_REASONING_EFFORT: ${SEALAI_V2_EVAL_JUDGE_REASONING_EFFORT:-low}
      SEALAI_V2_EVAL_JUDGE_MAX_OUTPUT_TOKENS: ${SEALAI_V2_EVAL_JUDGE_MAX_OUTPUT_TOKENS:-1024}
      SEALAI_V2_EXECUTION_POLICY_ENABLED: ${SEALAI_V2_EXECUTION_POLICY_ENABLED:-true}
      SEALAI_V2_STRUCTURED_ANSWER_ENABLED: ${SEALAI_V2_STRUCTURED_ANSWER_ENABLED:-true}
      # Cache activation is fail-closed: OFF unless explicitly enabled, and Settings then requires
      # a release-bound canonical authority digest. The empty fallback is deliberately not an epoch.
      SEALAI_V2_EXACT_ANSWER_CACHE_ENABLED: ${SEALAI_V2_EXACT_ANSWER_CACHE_ENABLED:-false}
      SEALAI_V2_KNOWLEDGE_AUTHORITY_EPOCH: ${SEALAI_V2_KNOWLEDGE_AUTHORITY_EPOCH:-}
      # SSoT M15/G5: explicit product activation gates. Both fail closed until
      # their independent evidence, review, and release requirements are met.
      SEALAI_V2_KNOWLEDGE_MODE_ENABLED: ${SEALAI_V2_KNOWLEDGE_MODE_ENABLED:-false}
      SEALAI_V2_KNOWLEDGE_REVIEW_ENABLED: ${SEALAI_V2_KNOWLEDGE_REVIEW_ENABLED:-false}
      SEALAI_V2_CASE_DECISION_RECORDS_ENABLED: ${SEALAI_V2_CASE_DECISION_RECORDS_ENABLED:-false}
      SEALAI_V2_CAPABILITY_PROFILES_ENABLED: ${SEALAI_V2_CAPABILITY_PROFILES_ENABLED:-false}
      SEALAI_V2_MANUFACTURER_FIT_ENABLED: ${SEALAI_V2_MANUFACTURER_FIT_ENABLED:-false}
      SEALAI_V2_MANUFACTURER_HANDOFF_ENABLED: ${SEALAI_V2_MANUFACTURER_HANDOFF_ENABLED:-false}
      MISTRAL_API_KEY: ${MISTRAL_API_KEY:?MISTRAL_API_KEY required for the V2 Mistral roles}
      # LangSmith observability (V2 tracing seam: wrap_openai + @traceable, fail-open/observation-only).
      # Values from .env.prod via --env-file interpolation; default OFF so a missing config never traces.
      # ENDPOINT must be NON-EMPTY: the SDK raises "API URL cannot be empty" on an empty value instead
      # of using the cloud, so the default is the US cloud (EU accounts set LANGSMITH_ENDPOINT in
      # .env.prod). The eval never traces (run_eval.sh unsets LANG*); a LangSmith outage never blocks a
      # turn (background submission).
      LANGSMITH_TRACING: ${LANGSMITH_TRACING:-false}
      LANGSMITH_API_KEY: ${LANGSMITH_API_KEY:-}
      LANGSMITH_PROJECT: ${LANGSMITH_PROJECT:-sealai-production}
      LANGSMITH_ENDPOINT: ${LANGSMITH_ENDPOINT:-https://api.smith.langchain.com}
      # Isolation (build-spec §3/§7): own Qdrant collection + own Postgres DB. V2 has no Redis,
      # Celery, or Kombu dependency; the durable outbox is Postgres-backed.
      # SEALAI_V2_DATABASE_URL backs memory layers 1-3 + the L4 cross-session seam with the durable
      # SQLAlchemy adapters (system-of-record) — a DEDICATED `sealai_v2` database in the shared
      # postgres instance (own tables, own snapshot/restore unit; the V1 `sealai` DB is never
      # touched). Unset → the in-process store (offline). Values are never logged.
      SEALAI_V2_DATABASE_URL: postgresql+psycopg2://${SEALAI_V2_DB_USER:?SEALAI_V2_DB_USER required - use the dedicated V2 database role}:${SEALAI_V2_DB_PASSWORD:?SEALAI_V2_DB_PASSWORD required - provision only through GATE-02/GATE-07}@postgres:5432/sealai_v2
      QDRANT_URL: ${QDRANT_URL:-http://qdrant:6333}
      SEALAI_V2_QDRANT_COLLECTION: ${SEALAI_V2_QDRANT_COLLECTION:-sealai_v2_knowledge_v1}
      SEALAI_V2_MEMORY_QDRANT_COLLECTION: ${SEALAI_V2_MEMORY_QDRANT_COLLECTION:-sealai_v2_memory}
      # Phase-1 Qdrant semantic retrieval (eval-validated by adjudicated run v21-qdrant-gate-4).
      # Flip via .env.prod (SEALAI_V2_RETRIEVER_BACKEND=qdrant); the retriever FAIL-SAFES to the
      # in-process keyword matcher on a missing dep / unreachable Qdrant / missing collection.
      SEALAI_V2_RETRIEVER_BACKEND: ${SEALAI_V2_RETRIEVER_BACKEND:-in_process}
      SEALAI_V2_QDRANT_URL: ${SEALAI_V2_QDRANT_URL:-http://qdrant:6333}
      SEALAI_V2_QDRANT_API_KEY: ${QDRANT_API_KEY:?QDRANT_API_KEY required - rotate/provision only through GATE-02/GATE-05}
      SEALAI_V2_EMBED_CACHE_DIR: ${SEALAI_V2_EMBED_CACHE_DIR:-/app/models}
      # Wave 2 RAG: embedding provider. PROD = openai text-embedding-3-small (1536-dim, RAM-safe — the
      # local e5-large OOM'd the 7.6 GB host; the openai API embeds with NO local model). DATA leaves the
      # box for the embed call (it is an API). fastembed/e5-large is the optional offline alternative (its
      # ONNX cache is the :ro mount below). Prefixes are EMPTY for openai; SINGLE-dash so an explicit
      # empty value in .env.prod is PRESERVED (a `:-` would force the e5 "query:/passage:" prefix back).
      SEALAI_V2_EMBED_PROVIDER: ${SEALAI_V2_EMBED_PROVIDER:-openai}
      SEALAI_V2_EMBED_MODEL: ${SEALAI_V2_EMBED_MODEL:-text-embedding-3-small}
      SEALAI_V2_EMBED_QUERY_PREFIX: "${SEALAI_V2_EMBED_QUERY_PREFIX-query: }"
      SEALAI_V2_EMBED_PASSAGE_PREFIX: "${SEALAI_V2_EMBED_PASSAGE_PREFIX-passage: }"
      # Hybrid retrieval (dense+sparse BM25, RRF-fused, optional cross-encoder rerank): wired but OFF by
      # default — flipping it on needs a Qdrant collection migration (re-ingest with the new "sparse"
      # named vector), an explicit owner-authorized ops step. Byte-identical when unset.
      SEALAI_V2_QDRANT_HYBRID_ENABLED: ${SEALAI_V2_QDRANT_HYBRID_ENABLED:-false}
      SEALAI_V2_QDRANT_SPARSE_MODEL: ${SEALAI_V2_QDRANT_SPARSE_MODEL:-Qdrant/bm25}
      SEALAI_V2_QDRANT_HYBRID_CANDIDATE_LIMIT: ${SEALAI_V2_QDRANT_HYBRID_CANDIDATE_LIMIT:-128}
      SEALAI_V2_QDRANT_RERANK_ENABLED: ${SEALAI_V2_QDRANT_RERANK_ENABLED:-false}
      SEALAI_V2_QDRANT_RERANK_MODEL: ${SEALAI_V2_QDRANT_RERANK_MODEL:-jinaai/jina-reranker-v2-base-multilingual}
      SEALAI_V2_QDRANT_RERANK_CANDIDATES: ${SEALAI_V2_QDRANT_RERANK_CANDIDATES:-20}
      # Medium Intelligence Phase-2: L1-NEUTRAL display data (never fed to L1/L3 → golden byte-identical).
      SEALAI_V2_MEDIUM_INTEL_ENABLED: ${SEALAI_V2_MEDIUM_INTEL_ENABLED:-false}
      # sealingAI Memory Architecture V1.0 (Patch 8): L1-NEUTRAL in this patch (context_sources on the
      # response, NOT yet injected into the L1 prompt — see memory/context_assembler.py). Needs both
      # database_url AND qdrant_url set to actually construct a service; off/unset → fully inert.
      SEALAI_V2_MEMORY_CONTEXT_ENABLED: ${SEALAI_V2_MEMORY_CONTEXT_ENABLED:-false}
      SEALAI_V2_SUPPRESS_CALC_FOR_NON_KERNEL_ROUTES_ENABLED: ${SEALAI_V2_SUPPRESS_CALC_FOR_NON_KERNEL_ROUTES_ENABLED:-false}
      SEALAI_V2_OUTBOX_POLL_INTERVAL_S: ${SEALAI_V2_OUTBOX_POLL_INTERVAL_S:-10}
      SEALAI_V2_OUTBOX_BATCH_SIZE: ${SEALAI_V2_OUTBOX_BATCH_SIZE:-50}
      SEALAI_V2_OUTBOX_MAX_ATTEMPTS: ${SEALAI_V2_OUTBOX_MAX_ATTEMPTS:-5}
      SEALAI_V2_OUTBOX_CLAIM_TIMEOUT_S: ${SEALAI_V2_OUTBOX_CLAIM_TIMEOUT_S:-300}
      # Kandidaten-Spezifikation (Produktspec v3.1): wired but OFF by default — owner governance gate
      # (expert Fachfreigabe + DIN-Lizenz + UI-Sprache). L1-neutral render surface; flip when cleared.
      SEALAI_V2_PRODUKTSPEC_ENABLED: ${SEALAI_V2_PRODUKTSPEC_ENABLED:-false}
      # 2026-07-04 routing/extraction audit: L1 pack suggestion + free-text medium hint, wired but
      # OFF by default (annotate-only via the understand stage; never gates/routes). Byte-identical
      # when unset.
      SEALAI_V2_PACK_SUGGESTION_ENABLED: ${SEALAI_V2_PACK_SUGGESTION_ENABLED:-false}
      SEALAI_V2_ADAPTIVE_INTERVIEW_ENABLED: ${SEALAI_V2_ADAPTIVE_INTERVIEW_ENABLED:-false}
      SEALAI_V2_ADAPTIVE_INTERVIEW_SHADOW_ENABLED: ${SEALAI_V2_ADAPTIVE_INTERVIEW_SHADOW_ENABLED:-false}
      SEALAI_V2_ADAPTIVE_INTERVIEW_PACK_RWDR_ENABLED: ${SEALAI_V2_ADAPTIVE_INTERVIEW_PACK_RWDR_ENABLED:-false}
      SEALAI_V2_ADAPTIVE_INTERVIEW_SHADOW_REPORTING_ENABLED: ${SEALAI_V2_ADAPTIVE_INTERVIEW_SHADOW_REPORTING_ENABLED:-false}
      # V2.2 INC-COVERAGE-GATE: deterministic case-level coverage_status, wired but OFF by default —
      # goes productive only when the extended eval ruler is green (I-CAL-1). Byte-identical when unset.
      SEALAI_V2_COVERAGE_GATE_ENABLED: ${SEALAI_V2_COVERAGE_GATE_ENABLED:-false}
      # INC-BASELINE-HARDENING: RWDR shaft-Ø derivation + speed-trap/unclear-medium prompt discipline,
      # wired but OFF by default. Byte-identical when unset.
      SEALAI_V2_BASELINE_HARDENING_ENABLED: ${SEALAI_V2_BASELINE_HARDENING_ENABLED:-false}
      # INC-NARRATOR-CONTRACT: L1 renders the kernel answer-contract + claim-level guard (regenerate-on-BLOCK).
      # Activated 2026-06-29 via eval-REPLAY narrator-contract-replay (tree 98edd308, L1 gpt-5.1).
      SEALAI_V2_RESPONSE_CONTRACT_ENABLED: ${SEALAI_V2_RESPONSE_CONTRACT_ENABLED:-false}
      # P0-B: widens the claim-level output_guard to non-Gegencheck turns (guard-only contract, NEVER
      # passed to generate() — see response_contract.build_guard_contract's docstring). Activated
      # 2026-07-03 after the targeted offline eval (eval/general_guard_eval.py) showed overblock_rate=0.0
      # on realistic grounded answers, plus 3 confirmed known-limitation cases (still correctly blocks).
      SEALAI_V2_RESPONSE_CONTRACT_GENERAL_GUARD_ENABLED: ${SEALAI_V2_RESPONSE_CONTRACT_GENERAL_GUARD_ENABLED:-false}
      # Material-Parameter-Tabelle: grounded kennwerte rendered as a table (numbers live in the kernel seed)
      SEALAI_V2_MATERIAL_PARAM_TABLE_ENABLED: ${SEALAI_V2_MATERIAL_PARAM_TABLE_ENABLED:-false}
      # Phase 2D (PR #185, 2026-07-08 governance reconciliation): conservative deterministic-first
      # route classification + the smalltalk_navigation-only compact prompt/L3-bypass path. Same
      # compose-passthrough gap as the auth-role/kill-switch flags above (settings.py default False,
      # but a .env.prod override would have silently done nothing without this line) — closing it
      # here. Defaults match settings.py's own defaults, so this is additive-only, zero behavior
      # change until the owner explicitly sets both true in .env.prod.
      SEALAI_V2_ROUTE_OPTIMIZATION_ENABLED: ${SEALAI_V2_ROUTE_OPTIMIZATION_ENABLED:-false}
      SEALAI_V2_ROUTE_PROMPT_FAMILIES_ENABLED: ${SEALAI_V2_ROUTE_PROMPT_FAMILIES_ENABLED:-false}
      # Phase 3A (PR #202): smalltalk-only live token streaming. Same compose-passthrough gap as
      # the two flags directly above -- settings.py defaults False, but a .env.prod override would
      # silently do nothing without this line. Additionally gated behind
      # route_optimization_enabled + route_prompt_families_enabled + the existing zero-signal
      # smalltalk_prompt_active boolean in pipeline.py -- setting this alone does not stream
      # anything if those are unset. Default false, additive-only.
      SEALAI_V2_SMALLTALK_TOKEN_STREAMING_ENABLED: ${SEALAI_V2_SMALLTALK_TOKEN_STREAMING_ENABLED:-false}
      # Phase 3B (PR #206): draft-token streaming for EVERY route's L1 generation call (not just
      # smalltalk). Same compose-passthrough gap as every flag above. This streams a
      # non-authoritative PREVIEW of the raw LLM draft only -- the delivered/verified answer still
      # requires the full output_guard + L3 verify pipeline unchanged for every non-smalltalk route;
      # this flag never skips or weakens verification, it only adds an observability/UX channel
      # around the existing generate() calls. Default false, additive-only.
      SEALAI_V2_DRAFT_TOKEN_STREAMING_ENABLED: ${SEALAI_V2_DRAFT_TOKEN_STREAMING_ENABLED:-false}
      # Paperless auto-ingestion (POST /internal/rag/ingest, api/routes/rag_ingest.py): fetches
      # a newly consumed rag:enabled document + LLM-extracts a DRAFT Fachkarte (never reviewed).
      # Values are never logged; unset -> the route fails safe (paperless_not_configured / 401).
      PAPERLESS_URL: ${PAPERLESS_URL:-}
      PAPERLESS_TOKEN: ${PAPERLESS_TOKEN:-}
      PAPERLESS_WEBHOOK_TOKEN: ${PAPERLESS_WEBHOOK_TOKEN:-}
    volumes:
    # Pre-staged e5-large ONNX cache (FastEmbed) — read-only; avoids a ~2GB cold download on the
    # health-gated recreate. uid 10001 reads via the bind mount (the cache contents are world-readable).
    - /home/thorsten/sealai-v2-fastembed:/app/models:ro
    expose:
    - '8001'
    healthcheck:
      test:
      - CMD-SHELL
      - curl -fsS http://127.0.0.1:8001/health >/dev/null
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      backend_network:
        aliases:
        - sealai-backend-v2
      backend_identity_network: {}
      postgres_backend_network: {}
      qdrant_backend_network: {}
      backend_metrics_network: {}
      backend_egress_network:
        gw_priority: 1
    read_only: true
    tmpfs:
    - /tmp:rw,noexec,nosuid,nodev,uid=10001,gid=10001,mode=1770
    security_opt:
    - no-new-privileges:true
    cap_drop:
    - ALL
    pids_limit: ${BACKEND_V2_PIDS_LIMIT:?BACKEND_V2_PIDS_LIMIT required after bounded-load measurement}
    mem_limit: ${BACKEND_V2_MEMORY_LIMIT:?BACKEND_V2_MEMORY_LIMIT required after bounded-load measurement}
    cpus: ${BACKEND_V2_CPU_LIMIT:?BACKEND_V2_CPU_LIMIT required after bounded-load measurement}
    logging:
      driver: local
      options:
        max-size: ${CONTAINER_LOG_MAX_SIZE:-10m}
        max-file: ${CONTAINER_LOG_MAX_FILES:-5}
  # Durable queue consumer. It extends the exact backend-v2 image and allow-listed
  # environment, but has no HTTP port and no API responsibility. Keeping it as a
  # separate container makes outbox delivery restart-safe without adding a hidden
  # scheduler to the request process.
  backend-v2-worker:
    container_name: backend-v2-worker
    profiles:
    - v2
    extends:
      service: backend-v2
    command:
    - python
    - -m
    - sealai_v2.memory.outbox_daemon
    ports: !reset []
    expose: !reset []
    networks: !override
      postgres_backend_network: {}
      qdrant_backend_network: {}
      backend_egress_network:
        gw_priority: 1
    pids_limit: ${BACKEND_V2_WORKER_PIDS_LIMIT:?BACKEND_V2_WORKER_PIDS_LIMIT required after bounded-load measurement}
    mem_limit: ${BACKEND_V2_WORKER_MEMORY_LIMIT:?BACKEND_V2_WORKER_MEMORY_LIMIT required after bounded-load measurement}
    cpus: ${BACKEND_V2_WORKER_CPU_LIMIT:?BACKEND_V2_WORKER_CPU_LIMIT required after bounded-load measurement}
    healthcheck:
      test:
      - CMD
      - python
      - -m
      - sealai_v2.memory.outbox_daemon
      - --healthcheck
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    depends_on:
      backend-v2:
        condition: service_healthy
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
volumes:
  backend-data: null
  qdrant_snapshots: null
networks:
  postgres_keycloak_network:
    internal: true
  postgres_backend_network:
    internal: true
  qdrant_backend_network:
    internal: true
  redis_network:
    internal: true
  document_network:
    internal: true
  observability_network:
    internal: true
  backend_metrics_network:
    internal: true
  strapi_postgres_network:
    external: true
    name: ${STRAPI_POSTGRES_NETWORK_NAME:?STRAPI_POSTGRES_NETWORK_NAME required for the scoped cross-project bridge}
  edge_network: {}
  frontend_network:
    internal: true
  backend_network:
    internal: true
  keycloak_edge_network:
    internal: true
  backend_identity_network:
    internal: true
  frontend_egress_network: {}
  backend_egress_network: {}
  keycloak_egress_network: {}
