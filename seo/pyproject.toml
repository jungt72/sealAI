[project]
name = "sealai-seo"
version = "0.1.0"
description = "Deterministic GSC-only SEO data pipeline for SealAI."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
