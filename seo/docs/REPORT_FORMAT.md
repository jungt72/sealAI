# Report Format

Reports are Markdown files with YAML frontmatter.

Daily anomaly reports detect movement and do not prove causes.

Weekly quick-win reports identify query/page candidates where GSC shows impressions, average position 5-20, and CTR below deterministic thresholds.

Report data is based on GSC Search Analytics API signals, not a complete raw export.
