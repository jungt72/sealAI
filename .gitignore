# Env files
#
# Real env files, host-local overrides, backups, and rollback snapshots must
# never be committed. Example/template files are allowed only when they contain
# placeholders.
.env
.env.*
*.env
*.env.*
**/.env
**/.env.*
**/*.env
**/*.env.*
*.env.bak
*.env.backup
*.env.rollback*
*.env.save*
.env*.bak*
.env*.backup*
.env*.rollback*
.env*.save*
!.env.example
!.env.prod.example

# Node / Next
node_modules/
.next/
out/
frontend/.next/
frontend/node_modules/
.pnpm-store/

# Python
__pycache__/
*.py[cod]
.venv*
backend/.venv/
.venv_audit/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Builds & Artefakte
dist/
build/
*.save.*
*.backup*
*.bak*
*.tmp
*.log
*.orig

# Local scratch
*.sql
# Legacy one-off frontend scratch copy. Canonical frontend sources are
# frontend/ (public Next frontend) and frontend-v2/ (dashboard SPA).
frontend-cockpit-deploy/
# Exception: audit schema dumps ARE allowed (Gate 0→1 artifacts, no user data)
!audits/sealai_schema_*.sql
*.docx
*.tx_
backups/
sealai-frontend@0.1.0

# Simulation / Job Artefakte
mein_job.*
JOBNAME.*
export.fbd
frontend/public/files/mein_job.*

# Test artefacts
tests/e2e_consult*.sh
coverage/
.coverage*

# Misc
.DS_Store
._*
*.pem
sealai_inventory_*/
code_dump.part.*
*.tar.gz
sidebar_chat_dump_*
software_versions.txt
system_info.txt
test_llm.py
test_langchain.py
test_stream.py
tests/test_consult_e2e.py
test_input.json
.trash/
_audits/
frontend_backup_*/
_deploy_backups/
_release_backups/

# editor/backup files
*.save
*.save.*
*.backup
*.backup_*
*.bak
*.swp
*~

# dumps / exports / secrets
code_dump_*.txt
*.dump
*.log
realm-export*.json
*realm*backup*.json
keycloak-realm-backup/
!keycloak/realm-export.json
!keycloak/import/realm-export.json

# runtime data
/data/

# local secrets (never commit)
secrets/
# Keycloak client-secret export — rotated + untracked (audit 2026-06-05 Risk #1).
# The secret is injected at runtime via KEYCLOAK_CLIENT_SECRET, never committed.
client.json

# accidental Windows paths from copy/paste
C:\Users\*

# local env files
.env.dev

# Paperless runtime
paperless/data/
paperless/media/

# Next generated
frontend/next-env.d.ts
frontend/tsconfig.tsbuildinfo
frontend/public/robots.txt
frontend/public/sitemap*.xml

# Restore / diagnostics artefacts
nginx/*.pre-restore-*
nginx/certs/
/serverinfo.json
/401
/BFF-
/Backend-Auth-Flow
/Session-
/Token-Pfad

# Local concept material
konzept/

erpnext-stack/
.claude/settings.local.json
.claude/.gate-logs/
_archive/

# Legacy local backups — removed from git, must not be re-committed (audit 2026-06-05 §4).
# Previously ignored only via local .git/info/exclude (per-clone, not shared).
archive/
langgraph_backup/

scratch/
.claude/plans/
ops/increments/
ops/ESCALATION.md

# Fachkarten-Ingestion review queue (transient — owner promotes to the seed)
ops/fachkarten_drafts/
.runtime/
