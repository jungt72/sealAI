[pytest]
testpaths = tests
addopts = -m "not e2e"  -q

markers =
    e2e: end-to-end tests requiring a running server
    asyncio: marks tests as asyncio (automatically handled if asyncio_mode=auto)

asyncio_mode = auto
